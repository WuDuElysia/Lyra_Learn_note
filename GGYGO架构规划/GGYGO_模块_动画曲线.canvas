{
	"nodes": [
		{"id":"nav","type":"text","text":"← [[GGYGO_模块_移动与位移.canvas|返回移动层]]　·　[[GGYGO_架构总览.canvas|总览]]　·　文字计划 [[计划_移动与动作位移]]\n\n# 动画曲线：从资产到运行时消费\n\n这张图只说明统一的数据流，不再展开某一个转身动作的逐帧状态机。\n\n原则：**资产管线负责把数据变可靠，采样器负责读一次，消费者只读同一份快照。**","x":-1260,"y":-900,"width":660,"height":300,"color":"3"},
		{"id":"source","type":"text","text":"## 1. 资产准备与烘焙\n\n逐帧位移/旋转来自 FBX 外部 `Root` 节点；per-clip JSON 只提供 ClipLength、LoopTime、平均速度等可信标量。\n\n```text\nFBX + per-clip JSON\n  → ZZZStripRootMotion       // Bip001 原地化，避免双重位移\n  → anim_rootmotion_extract  // 提取并转成 UE 坐标/单位\n  → bake_*_curves            // 写入 AnimSequence FloatCurve\n  → verify_*_curves          // 全量回读验证\n```\n\n`anim_settings.json` 中未填充的 RootMotion 数组不能作为逐帧真源。原地化工具不幂等，同一资产只能执行一次。","x":-1260,"y":-470,"width":600,"height":480,"color":"4"},
		{"id":"contract","type":"text","text":"## 2. 曲线契约\n\n运行时当前消费 9 条核心曲线：\n\n| 曲线 | 语义 |\n| --- | --- |\n| `RootMotion_Speed` | cm/s，当前帧目标速度 |\n| `PosX/PosY`、`Dist` | 相对片段起点的累计位置/路程 |\n| `Yaw` | 解开 ±180 折叠后的累计转角 |\n| `DirX/DirY` | 片段起点局部坐标中的方向 |\n| `Cfg_ClipLength/LoopTime` | 片段边界与循环配置 |\n\n统一约定：X 前、Y 右、Z 上；厘米/度；位置、方向和 Yaw 都以**动画片段入口**为基准。世界方向只在消费端用 `EntryYaw` 转一次。","x":-500,"y":-470,"width":600,"height":500,"color":"4"},
		{"id":"sample","type":"text","text":"## 3. 每帧只采样一次\n\n`FGGYGOAnimCurveSampler` 在 `UGGYGOCharacterMovementComponent::TickComponent` 中、`Super` 之前读取 AnimInstance 曲线，产出一份 `CurveMotion` 快照：\n\n```text\nCurveMotion = {\n  bValid, Speed, DeltaPosition, Direction,\n  YawTotal, YawDelta, ClipLength, LoopTime\n}\n```\n\n放在 `Super` 前是为了让本帧移动立即消费；不能放进可能 sub-step 多次的回调，否则跨帧差分会被重复推进。\n\n片段/循环边界由单调 `Dist` 的回退识别，不用可能往返的 PosX/PosY 猜。曲线缺失时 `bValid=false`，下游必须走固定速度或普通移动回退。","x":260,"y":-470,"width":620,"height":520,"color":"1"},
		{"id":"consumers","type":"text","text":"## 4. 消费者集中在一个边界内\n\n| 消费位置 | 读取内容 | 作用 |\n| --- | --- | --- |\n| `GetMaxSpeed` | Speed | 输入驱动走跑的速度上限 |\n| Curve RootMotionSource | Speed + Direction + EntryYaw | 刹停、转身等动画驱动水平位移 |\n| `PhysicsRotation` | YawDelta | 仅在曲线接管朝向时累加旋转 |\n| CMC 状态解算 | Dist / Direction / Yaw | 推导停步、转身相位与回退条件 |\n| AnimInstance | CMC 已推导的状态 | 选择动画，不反向写移动权威 |\n\n转身只是这里的一个消费者案例，不再维护第二套“转身曲线读取链”。GAS、BehaviorTree、AnimGraph 节点也不各自重新 `GetCurveValue`。","x":1020,"y":-470,"width":640,"height":520,"color":"5"},
		{"id":"boundary","type":"text","text":"## 结构与网络边界\n\n- 曲线原始值只由 Sampler 读取；外部只见 `CurveMotion`。\n- 坐标/单位换算与 ±180 解折叠只在烘焙阶段做一次。\n- 相位判断集中在 CMC；RootMotionSource 只执行速度/方向。\n- 动作专属阈值可以配置，但不能产生第二套采样缓存。\n\n网络不复制整套 FloatCurve，只保存影响移动重演的最小状态：控制标志、入口朝向、相位和必要曲线运动量。曲线源生效期间禁止 move 合并。\n\n服务器若不评估动画，必须从 NetworkMove 恢复必要运动量，不能假设服务端自行读到客户端曲线。若新增动作需要大量跨模块连线，先扩充统一快照或 CMC 状态。","x":260,"y":220,"width":620,"height":500,"color":"2"},
		{"id":"verify","type":"text","text":"## 最小验证与排查\n\n**资产验证**：ClipLength > 0、末帧回读一致、循环周期正确、原地化后骨架不再携带水平位移/yaw。\n\n**运行时验证**：\n- 曲线无效时能回落，不会速度归零；\n- 走跑脚步与 Speed 对齐；\n- 刹停/转身位移只出现一次，不会 360° 或双倍位移；\n- 离地时 Override 源退出并保留水平动量；\n- Dedicated Server 下无持续位置纠正。\n\n高频症状：双倍位移先查是否漏做/重复做 Strip；一直走不跑先查 BlendSpace1D 的 X 输入；动画播一次冻结先查循环标记。更细的数值与未验证项留在 [[计划_实施状态]]，不堆进本图。","x":1020,"y":220,"width":640,"height":480,"color":"2"}
	],
	"edges": [
		{"id":"e-source-contract","fromNode":"source","fromSide":"right","toNode":"contract","toSide":"left","label":"烘焙为统一语义"},
		{"id":"e-contract-sample","fromNode":"contract","fromSide":"right","toNode":"sample","toSide":"left","label":"按契约读取"},
		{"id":"e-sample-consumers","fromNode":"sample","fromSide":"right","toNode":"consumers","toSide":"left","label":"唯一 CurveMotion 快照"},
		{"id":"e-sample-boundary","fromNode":"sample","fromSide":"bottom","toNode":"boundary","toSide":"top","label":"统一状态进入预测"},
		{"id":"e-consumers-verify","fromNode":"consumers","fromSide":"bottom","toNode":"verify","toSide":"top","label":"验证消费结果"}
	]
}
