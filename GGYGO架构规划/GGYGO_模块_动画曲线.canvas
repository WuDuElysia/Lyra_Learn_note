{
	"nodes":[
		{"id":"nav","type":"text","text":"← [[GGYGO_模块_移动与位移.canvas|返回移动层]]　·　[[GGYGO_架构总览.canvas|总览]]　·　目标计划 [[计划_动画与表现层#7. 曲线与权威移动的新边界|动画计划第 7 节]]\n\n# 动画曲线：资产数据与权威消费\n\n曲线烘焙契约继续保留，但运行时读取边界需要迁移。\n\n原则：普通 locomotion 不由 AnimGraph 反向决定；动画节奏必须驱动移动时，使用 C++ 可独立采样的 MotionProfile 或原生 RootMotion。","x":-1260,"y":-900,"width":660,"height":300,"color":"3"},
		{"id":"contract","type":"text","text":"## 2. 曲线契约\n\n运行时当前消费 9 条核心曲线：\n\n| 曲线 | 语义 |\n| --- | --- |\n| `RootMotion_Speed` | cm/s，当前帧目标速度 |\n| `PosX/PosY`、`Dist` | 相对片段起点的累计位置/路程 |\n| `Yaw` | 解开 ±180 折叠后的累计转角 |\n| `DirX/DirY` | 片段起点局部坐标中的方向 |\n| `Cfg_ClipLength/LoopTime` | 片段边界与循环配置 |\n\n统一约定：X 前、Y 右、Z 上；厘米/度；位置、方向和 Yaw 都以**动画片段入口**为基准。世界方向只在消费端用 `EntryYaw` 转一次。","x":-500,"y":-470,"width":600,"height":500,"color":"4"},
		{"id":"source","type":"text","text":"## 1. 资产准备与烘焙\n\n逐帧位移/旋转来自 FBX 外部 `Root` 节点；per-clip JSON 只提供 ClipLength、LoopTime、平均速度等可信标量。\n\n```text\nFBX + per-clip JSON\n  → ZZZStripRootMotion       // Bip001 原地化，避免双重位移\n  → anim_rootmotion_extract  // 提取并转成 UE 坐标/单位\n  → bake_*_curves            // 写入 AnimSequence FloatCurve\n  → verify_*_curves          // 全量回读验证\n```\n\n`anim_settings.json` 中未填充的 RootMotion 数组不能作为逐帧真源。原地化工具不幂等，同一资产只能执行一次。","x":-1400,"y":-470,"width":600,"height":480,"color":"4"},
		{"id":"sample","type":"text","text":"## 3. 运行时读取边界\n\n**当前兼容链路**\n`FGGYGOAnimCurveSampler` 在 CMC Tick、`Super` 前读取当前 AnimInstance 曲线，生成 `CurveMotion`。它保证同帧只读一次，但仍让权威移动依赖 AnimGraph。\n\n**目标链路**\n离线烘焙结果进入强类型 `MotionProfile`：\n```text\nProfileId + Time\n  → C++ 独立采样 Speed/Delta/Yaw\n  → CMC / RootMotionSource\n```\n普通走跑不再采样动画曲线，由 CMC 决定速度，AnimBP 通过播放速率、Stride Warping 或 Distance Matching 适配。\n\n迁移完成前保留旧 Sampler；完成后 CMC 不再调用 `GetAnimInstance()->GetCurveValue()`。","x":360,"y":-470,"width":620,"height":520,"color":"1"},
		{"id":"consumers","type":"text","text":"## 4. 目标消费者边界\n\n| 消费者 | 输入 | 职责 |\n| --- | --- | --- |\n| 普通 CMC | MovementSet + 输入 + 地形 | 走跑速度、加速度、摩擦与碰撞权威 |\n| MotionProfile 播放器 | ProfileId + Time | 转身/刹停的速度、位移与 Yaw |\n| RootMotionSource | Profile 采样结果 | 执行需要动画节奏的短时移动 |\n| AnimationStateFrame | CMC/GAS 已推导状态 | 向 AnimBP 发布语义事实 |\n| AnimBP / Linked Layer | StateFrame + AnimProfile | 选择动画、混合与表现记忆 |\n\nGAS、BehaviorTree 和 AnimGraph 不各自重新读取权威曲线。角色差异放 Profile/Layer，不复制运行时算法。","x":1360,"y":-470,"width":640,"height":520,"color":"5"},
		{"id":"verify","type":"text","text":"## 最小验证与排查\n\n**资产验证**：ClipLength > 0、末帧回读一致、循环周期正确、原地化后骨架不再携带水平位移/yaw。\n\n**运行时验证**：\n- 曲线无效时能回落，不会速度归零；\n- 走跑脚步与 Speed 对齐；\n- 刹停/转身位移只出现一次，不会 360° 或双倍位移；\n- 离地时 Override 源退出并保留水平动量；\n- Dedicated Server 下无持续位置纠正。\n\n高频症状：双倍位移先查是否漏做/重复做 Strip；一直走不跑先查 BlendSpace1D 的 X 输入；动画播一次冻结先查循环标记。更细的数值与未验证项留在 [[计划_实施状态]]，不堆进本图。","x":1360,"y":240,"width":640,"height":480,"color":"2"},
		{"id":"boundary","type":"text","text":"## 结构与网络边界\n\n- 坐标、单位换算与 ±180 解折叠只在离线烘焙阶段做一次；\n- MotionProfile 是权威运行时数据，不依赖 Mesh 是否求值动画；\n- 相位判断集中在 CMC；RootMotionSource 只执行采样结果；\n- AnimationStateFrame 只发布结果，不成为第二个移动状态机；\n- 动作阈值允许配置，但不能产生第二套采样缓存。\n\n网络不复制整套 FloatCurve，也不优先逐帧传实时曲线量。预测重演保存最小确定状态：ProfileId、起始时刻/播放时间、入口朝向、相位与控制标志；具体字段在 Anim-D 阶段用 Dedicated Server 验证后冻结。","x":360,"y":220,"width":620,"height":500,"color":"2"}
	],
	"edges":[
		{"id":"e-source-contract","fromNode":"source","fromSide":"right","toNode":"contract","toSide":"left","label":"烘焙为统一语义"},
		{"id":"e-contract-sample","fromNode":"contract","fromSide":"right","toNode":"sample","toSide":"left","label":"烘成 Profile / 原生 RM"},
		{"id":"e-sample-consumers","fromNode":"sample","fromSide":"right","toNode":"consumers","toSide":"left","label":"确定性运行时数据"},
		{"id":"e-sample-boundary","fromNode":"sample","fromSide":"bottom","toNode":"boundary","toSide":"top","label":"统一状态进入预测"},
		{"id":"e-consumers-verify","fromNode":"consumers","fromSide":"bottom","toNode":"verify","toSide":"top","label":"验证消费结果"}
	]
}
