{
	"nodes":[
		{"id":"nav","type":"text","text":"← [[GGYGO_架构总览.canvas|返回总览]]　·　类清单见 [[模块参考]] 第 6 节\n\n# 相机层\n\n文件在 `Source/GGYGO/Camera/`。橙色 = 还缺的东西。\n\n**一句话**：镜头不是一个组件的参数，而是一叠「模式」每帧求值加权混合的结果，\n最后再叠一层「微调」。这样能力可以临时接管镜头并自动恢复，\n不必自己记住「我之前是什么视角」。","x":-1240,"y":-820,"width":660,"height":320,"color":"3"},

		{"id":"frame","type":"text","text":"## 每帧的入口\n\n引擎的 `APlayerCameraManager` 每帧向当前 ViewTarget 要视角，\n于是走到组件的 `GetCameraView`：\n\n```\nGetCameraView(dt, out DesiredView):\n    UpdateCameraModes()              // ① 推默认模式\n    Stack->EvaluateStack(dt, View)   // ② 求值 + 混合\n    UpdateCameraOffsetAlpha(dt)      // ③ 推进微调强度\n    ApplyCameraOffset(View)          // ④ 叠加微调\n\n    Controller->SetControlRotation(View.ControlRotation)\n    SetWorldLocationAndRotation(View.Location, View.Rotation)\n    DesiredView = { View.Location, View.Rotation, View.FieldOfView }\n```\n\n④ 在 ② 之后而不是混进 ② 里：微调调整的是**最终镜头**，\n若参与模式间加权会被其它模式的权重稀释。\n\n写回 `SetControlRotation` 是为了让「看向哪」与「前是哪个方向」一致 ——\n移动输入按控制朝向解析，不同步的话演出期间玩家的前后左右与画面不符。","x":-460,"y":-820,"width":660,"height":560,"color":"4"},

		{"id":"stack","type":"text","text":"## 模式栈：为什么索引 0 反而最后混入\n\n`Stack[0]` 是栈顶（最新推入）。求值时**从数组末尾往索引 0 混**，\n所以末尾是基准，索引 0 最后叠上去、权重满时完全遮盖下层。\n\n```\nPushCameraMode(X):\n    if Stack[0] == X: return          // 已在栈顶，无操作\n\n    contribution = 1\n    for i in 0..N-1:                  // 从栈顶往下找 X\n        if Stack[i] == X: found = i; break\n        contribution *= (1 - Stack[i].Weight)\n\n    if found: Stack.RemoveAt(found)   // 挪位置，不是新建\n    Stack.Insert(X, 0)\n\n    X.SetBlendWeight(N == 0 ? 1 : contribution)\n```\n\n`contribution` 是「X 之上那些模式还没遮住的部分」。\n新模式从这个值开始升权重，而不是从 0 开始 ——\n从 0 开始会让已经可见的画面先退回去再走过来，产生一次回跳。\n\n**栈底必须满权重**：它是最终视角的基准，不满会混进未初始化的值。\n\n权重升满的模式会把它下面的全部移除（已完全不可见，留着白算），\n所以栈不会随能力反复激活而无限增长。","x":320,"y":-820,"width":660,"height":620,"color":"4"},

		{"id":"blend","type":"text","text":"## 混合求值\n\n```\nEvaluateStack(dt, out View):\n    UpdateStack(dt)      // 每个模式各自 UpdateView + 推进权重\n    BlendStack(View)\n\nBlendStack(out View):\n    View = Stack[N-1].View               // 数组末尾 = 基准\n    for i = N-2 downto 0:                // 往索引 0 方向叠\n        View.Blend(Stack[i].View, Stack[i].Weight)\n```\n\n```\nView.Blend(Other, w):\n    if w <= 0: return\n    if w >= 1: *this = Other; return\n\n    Location    = Lerp(Location, Other.Location, w)\n    FieldOfView = Lerp(FieldOfView, Other.FieldOfView, w)\n\n    // 旋转先归一化再插值\n    Rotation += w * (Other.Rotation - Rotation).GetNormalized()\n    ControlRotation += w * (...).GetNormalized()\n```\n\n**旋转不能直接 Lerp 欧拉角**：跨越 ±180 度时会绕远路，\n表现为镜头猛甩一圈。归一化到 [-180,180] 再插值才对。","x":1120,"y":-820,"width":660,"height":580,"color":"4"},

		{"id":"restore","type":"text","text":"## 自动恢复：默认模式每帧都推\n\n```\nUpdateCameraModes():                      // 每帧\n    Mode = DetermineCameraModeDelegate.Execute()\n    Stack->PushCameraMode(Mode)\n\n// 委托绑在 AGGYGOHeroCharacter：\nDetermineCameraMode():\n    return PawnExt->GetPawnData()->DefaultCameraMode\n```\n\n看着多余，其实是**恢复机制的全部**：\n\n```\n能力激活    → 推 X，X 在栈顶权重从 contribution(=0) 起升\n              D 每帧被重推，权重 = 1 - X.Weight，逐渐降到 0\n              → 画面从 D 平滑过渡到 X\n\n能力结束    → 没人再推 X。D 每帧被推但已在栈顶 → 直接 return，\n              不再被重置权重，于是 D 自己升回 1\n              → 画面平滑回到 D，X 权重被顶到 0 后从栈里移除\n```\n\n所以能力**推一次就够**，不需要每帧推，也不需要显式弹栈。\n栈没有 Pop 操作，显式弹出反而会造成一次跳变。\n\n返回空是合法的：PawnData 还没到位时保持现状，\n而不是塞一个硬编码兜底模式（那会在初始化完成时产生可见跳变）。","x":-460,"y":-180,"width":660,"height":620,"color":"4"},

		{"id":"offset","type":"text","text":"## 镜头微调：不换模式的那一层\n\n```\nSetCameraOffset(NewOffset):\n    CameraOffset = NewOffset\n    bActive = true\n    // 不重置 Alpha：上一份可能还没回落完，\n    // 从当前强度续着过渡比归零重来更平滑\n\nClearCameraOffset():\n    bActive = false        // 保留数值，回落还要用它和 BlendOutTime\n\nUpdateCameraOffsetAlpha(dt):\n    T = bActive ? BlendInTime : BlendOutTime\n    Alpha = Clamp(Alpha ± dt / T, 0, 1)\n\nApplyCameraOffset(out View):\n    if Alpha <= 0 or Offset.IsNearlyZero(): return\n\n    // 局部空间：同一份配置在任何朝向下观感一致\n    View.Location += View.Rotation.RotateVector(\n                         Offset.LocationOffset * Alpha)\n    View.FieldOfView = Clamp(\n        View.FieldOfView + Offset.FOVDelta * Alpha, 5, 170)\n```\n\nAlpha 按时间线性推进，**不用 `FInterpTo`** ——\n后者是指数逼近，永远到不了端点，会残留一个极小的偏移量。\n\nFOV 必须钳制：误填大值算出 0 或负数会让投影矩阵失效。\n\n同一时刻只保留一份，再次施加直接替换。多份叠加要引用计数与撤销顺序，\n而实际需求是互斥的：两个都改镜头的能力同时激活时，后者应当覆盖前者。","x":320,"y":-140,"width":660,"height":660,"color":"4"},

		{"id":"ability","type":"text","text":"## 能力怎么用：按幅度分三层\n\n```\n// ① 微调 —— 每段攻击各配一份，不换模式\nGA_Attack_Heavy.CameraOffset = {\n    LocationOffset = (30, 0, 0),   // 正 X 拉近\n    FieldOfViewDelta = -5,         // 收窄，压缩透视\n    BlendInTime = 0.08, BlendOutTime = 0.25\n}\n\n// ② 换机位 —— 大招、处决特写、锁定\nGA_Ultimate.AbilityCameraMode = UCM_UltimateOrbit\n\n// ③ 过场 —— 不进栈，走 LevelSequence + SetViewTarget\n```\n\n```\nActivateAbility():                    // 基类自动应用\n    if AbilityCameraMode: SetCameraMode(AbilityCameraMode)\n    if !CameraOffset.IsNearlyZero(): ApplyCameraOffset(CameraOffset)\n    Super::ActivateAbility()          // 之后才触发蓝图事件\n\nEndAbility():                         // 统一撤销\n    ClearCameraOffset()\n    ClearCameraMode()                 // 只清运行时标记，不弹栈\n    Super::EndAbility()\n```\n\n**微调为什么不走换模式**：换模式会丢掉当前模式的内部状态\n（锁定的目标、穿墙规避的臂长恢复进度），而且栈按整份视角混合，\n连段中反复换模式会让镜头在两份视角之间来回过渡。\n\n**两者可叠**：`AbilityCameraMode` 换机位，`CameraOffset` 再在那个机位上偏移。\n\n在 `ActivateAbility` 里自动应用而不是让每个能力蓝图自己调：\n它们是声明式配置，配了就该生效。\n`EndAbility` 覆盖被组仲裁取消、被死亡取消、Avatar 销毁这些不走正常收尾的路径。","x":1120,"y":-180,"width":680,"height":700,"color":"4"},

		{"id":"config","type":"text","text":"## 配置与运行时状态必须分开\n\n```\n// 配置：EditDefaultsOnly，编辑器里填\nTSubclassOf<UGGYGOCameraMode> AbilityCameraMode;\nFGGYGOCameraOffset            CameraOffset;\n\n// 运行时：Transient，代码写\nUPROPERTY(Transient)\nTSubclassOf<UGGYGOCameraMode> ActiveCameraMode;\n```\n\n合成一个字段会出这个 bug：\n\n```\nUGGYGOGameplayAbility 是 InstancedPerActor —— 实例会复用\n\n第一次激活  → SetCameraMode(配置值) → 生效\n第一次结束  → ClearCameraMode() → 字段 = null\n第二次激活  → 字段已是 null → 永远不再接管相机\n```\n\n症状是「大招第一次有演出镜头，之后再也没有」，而且不报错。","x":-1240,"y":-380,"width":660,"height":480,"color":"4"},

		{"id":"thirdperson","type":"text","text":"## 第三人称模式的实现\n\n```\nUpdateView(dt):\n    Pivot = GetPivotLocation()      // Pawn->GetPawnViewLocation()，已含眼高\n    Rot   = GetPivotRotation()      // 控制朝向\n    Rot.Pitch = ClampAngle(Rot.Pitch, ViewPitchMin, ViewPitchMax)\n\n    View.Rotation = View.ControlRotation = Rot\n    View.FieldOfView = FieldOfView\n\n    Offset  = Rot.RotateVector(TargetOffset)   // 视线空间，不是角色空间\n    Desired = Pivot + Offset\n\n    if !bPreventPenetration:\n        View.Location = Desired; return\n\n    // 从枢轴往目标位置扫球，命中即有遮挡\n    Blocked = SweepSingleByChannel(Pivot, Desired,\n                  Sphere(PenetrationProbeRadius), ECC_Camera)\n    TargetRatio = Blocked ? Hit.Time : 1\n\n    if TargetRatio < CurrentRatio:\n        CurrentRatio = TargetRatio            // 立即拉近\n    else:\n        CurrentRatio = FInterpTo(CurrentRatio, TargetRatio,\n                                 dt, PenetrationRecoverySpeed)  // 平滑推远\n\n    View.Location = Pivot + Offset * CurrentRatio\n```\n\n**偏移用视线空间而非角色局部空间**：镜头应始终在视线后方，\n用角色朝向会让原地转身时镜头绕着角色转。\n\n**拉近立即、推远平滑**：平滑拉近意味着这几帧镜头仍在墙内会看到背面；\n立即推远会在经过门框、柱子时剧烈前后抽动。\n\n`CurrentArmLengthRatio` 跨帧保留，这也是模式实例必须复用而不是每次新建的原因。\n\n默认值：`TargetOffset = (-350, 0, 60)`、`FOV 85`、Pitch 限 ±70。","x":-1240,"y":180,"width":680,"height":700,"color":"4"},

		{"id":"control","type":"text","text":"## Rotation 与 ControlRotation 为什么分开\n\n```\nstruct FGGYGOCameraModeView {\n    FVector  Location;         // 镜头在哪\n    FRotator Rotation;         // 画面看向哪\n    FRotator ControlRotation;  // 「前」是哪个方向\n    float    FieldOfView;\n}\n```\n\n常规跟随时两者相同。分开的价值在演出：\n\n```\n大招环绕镜头：\n    View.Rotation = 环绕机位的朝向      // 画面绕着角色转\n    View.ControlRotation = 保持原值      // 玩家的前后左右不变\n```\n\n合成一个的话，演出镜头一转，玩家按「前」就会往画面新方向跑，\n出招结束时角色朝向已经乱了。","x":-460,"y":560,"width":660,"height":420,"color":"4"},

		{"id":"todo","type":"text","text":"## 还缺什么\n\n**锁定模式**未做。它是模式栈价值最大的场景：\n锁定期间放大招、大招结束回到锁定，这正是栈解决的嵌套。\n需要一个 `UGGYGOCameraMode_LockOn`，`UpdateView` 里把\n`Rotation` 指向目标而 `ControlRotation` 交给输入。\n\n**镜头抖动**未做。命中反馈需要它。UE 自带 `UCameraShakeBase`，\n可以经 `PlayerCameraManager->StartCameraShake` 走，不必进本层的栈 ——\n抖动是叠加在最终视角上的扰动，与「镜头在哪」是两件事。\n\n**过场镜头**未做，走 LevelSequence，不进栈。\n\n**没有相机模式蓝图子类**。`DA_Pawn_Pyrios.DefaultCameraMode` 直接指向\nC++ 类 `UGGYGOCameraMode_ThirdPerson`，用的是默认值。\n要按角色调镜头距离（大剑远、双刀近）时再派生蓝图子类。\n\n微调的实际手感（拉多近、收多少 FOV）尚未调过 ——\n它做成结构体配置就是为了不用改代码调参。","x":1120,"y":600,"width":680,"height":520,"color":"2"}
	],
	"edges":[
		{"id":"e-frame-stack","fromNode":"frame","fromSide":"right","toNode":"stack","toSide":"left","label":"② 求值"},
		{"id":"e-stack-blend","fromNode":"stack","fromSide":"right","toNode":"blend","toSide":"left","label":"栈内加权"},
		{"id":"e-frame-restore","fromNode":"frame","fromSide":"bottom","toNode":"restore","toSide":"top","label":"① 每帧推默认模式"},
		{"id":"e-frame-offset","fromNode":"frame","fromSide":"bottom","toNode":"offset","toSide":"top","label":"③④ 叠加微调"},
		{"id":"e-ability-offset","fromNode":"ability","fromSide":"left","toNode":"offset","toSide":"right","label":"CameraOffset"},
		{"id":"e-ability-stack","fromNode":"ability","fromSide":"top","toNode":"stack","toSide":"bottom","label":"AbilityCameraMode 推入栈"},
		{"id":"e-config-ability","fromNode":"config","fromSide":"right","toNode":"ability","toSide":"left","label":"字段拆分是这层的前提"},
		{"id":"e-restore-thirdperson","fromNode":"restore","fromSide":"left","toNode":"thirdperson","toSide":"right","label":"默认模式即它"},
		{"id":"e-thirdperson-control","fromNode":"thirdperson","fromSide":"right","toNode":"control","toSide":"left","label":"写两份朝向"},
		{"id":"e-ability-todo","fromNode":"ability","fromSide":"bottom","toNode":"todo","toSide":"top","label":"锁定与抖动待补"}
	]
}
