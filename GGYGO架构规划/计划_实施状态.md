# 实施状态与缺口

> 返回 [[计划蓝图]] · 当前代码事实见 [[模块参考]]

> 本文只记录当前实现与待办，不定义新的架构规则。

## 15. 实施结果

前面各章的 C++ 部分均已落地并编译通过。本章记录**实际做出来的东西与设计的差异**，
以及还需要在编辑器里做什么才能真正跑起来。

### 15.1 与计划的偏离

| 领域 | 计划 | 实际 | 原因 |
|---|---|---|---|
| 移动 | CMC 直接承接曲线驱动 | 固定 `WalkSpeed` / `RunSpeed` 与曲线速度**并存**，曲线不可用时回落到固定值 | 曲线只在有 `RM_*` 曲线的动画播放时有值。没有回落档时，任何一个未烘焙曲线的动画会让角色完全不能动 |
| 攻击 | `GA_Attack_Light` 完整动作 | 伤害 Execution、近战判定、连段缓冲齐备；**没有具体的攻击 GA** | 攻击 GA 的主体是 Montage 编排与判定窗口通知，那些是资产而非代码 |
| 位移解算 | `MotionWarpSolver` 解算位移终点 | **未实现** | 它的输入是"目标在哪、想停在哪"，需要先有锁定系统与真实的攻击动作才能定形态 |
| 队伍 | 队伍级 ASC（挂 PlayerState） | **未建**，只有 `UGGYGOSquadComponent` | 目前没有任何队伍共享属性，建了就是一个谁都不读的 ASC |
| ASC 归属 | 玩家 Slot 与可换形态 Boss 分别持有外置 ASC | **阶段 A/B 已完成**；`AGGYGOCharacterSlot` 与 `AGGYGOBossState` 均派生 `AGGYGOCombatantState` | 玩家 Slot 保持 Mixed；BossState 使用 Minimal，二者只复用 Avatar 生命周期，不复用队伍业务 |
| 联机 | Dedicated Server 实测预测与纠正 | 只补了转身状态的复制通道 | 联机验证需要能实际跑起来的场景，依赖 15.2 的资产配置 |

两项计划外的产出：

- **`.kiro/steering/code-comments.md`**：注释规范。核心是注释描述当下代码，
  不记录"从什么变成什么"—— 那属于 git log。历史型注释里被提及的类一旦删除，
  注释就变成读者查不到的死引用。
- **`InputTag` 根标签**：让"这个 Tag 是不是输入标签"能用一次层级匹配回答。

### 15.2 待配置项

以下是编辑器里的操作，代码侧已经准备好接口。**在这些配置完成前，项目跑起来会是一个空场景。**

**当前状态**：下表资产除物理材质外均已创建并接线完毕。已核对到位的链路：

- `DA_Pawn_Pyrios` 的 `PawnClass` / `InputConfig` / `MovementSet` /
  `AbilityGroupConfig` / `AbilitySets` / `DefaultCameraMode` 六个字段全部有值，
  其中 `DefaultCameraMode` 直接指向 C++ 类 `UGGYGOCameraMode_ThirdPerson`
  （`TargetOffset` 的默认值 `(-350, 0, 60)` 开箱可用，暂不需要蓝图子类）
- `DA_InputConfig_Default` 的 `NativeInputActions` 填了 Move / Look.Mouse /
  Look.Stick / Jump / Sprint / ForceWalk，`AbilityInputActions` 填了 Attack.Light
- `BP_PC_Pyrios` 父类为 `AGGYGOHeroCharacter`，HeroComponent 的
  `DefaultInputMappings` 配了 `IMC_Default`，CameraComponent 与 `ABP_Pyrios` 都在；
  旧蓝图遗留的 `SpringArm + Camera` 已删除，ViewTarget 上只有继承的
  `UGGYGOCameraComponent` 处于激活状态
- 相机模式由 HeroComponent 每帧返回“能力覆盖栈顶或 PawnData 默认模式”，每条能力覆盖带
  `FGameplayAbilitySpecHandle` 所有权；任意能力结束只移除自己的覆盖，因此嵌套接管能恢复上一层。
  CameraModeStack 只做实例复用与平滑混合。空栈阶段回退到
  `UCameraComponent::GetCameraView`，不会写入世界原点视图
- 2026-09-17 验证：`GGYGO` Win64 Development 独立 Game Target 完整编译、UHT 与链接通过；
  `BP_PC_Pyrios` 重新编译无警告，PIE 能正常装配并生成 `BP_PC_Pyrios_C_0`，无相机或蓝图运行错误
- `GGYGOGameData` 三个 GE 字段齐（伤害、治疗、自毁）
- Slot 改造完成后属性集随位置构造即就绪，生命系统已可用

**所以角色能生成、能操控、有相机与动画。真正挡住玩法验证的是攻击链**：
四个能力蓝图仍是空壳，只配了组身份与优先级，没有任何 Montage 引用。
按下攻击键会激活一个什么都不做的能力。物理材质也仍是新建未配置状态，
命中材质分流用不上。

**项目设置**

- `Config/DefaultInput.ini` 的 `DefaultInputComponentClass` 已改为 `GGYGOInputComponent`（已提交）
- 关卡的 GameMode Override 指向 `AGGYGOGameMode` 的蓝图子类（或直接用 C++ 类）

**需要创建的资产**

| 资产类型 | 用途 | 关键字段 |
|---|---|---|
| `UGGYGOInputConfig` | 按键与 InputTag 的对应 | `NativeInputActions` 填 `InputTag.Move` / `InputTag.Look.Mouse`；`AbilityInputActions` 填攻击、闪避等 |
| `UGGYGOMovementSet` | 移动手感 | `WalkSpeed` / `RunSpeed` / `WalkToRunHoldSeconds`；`bUseCurveDrivenSpeed` 默认开 |
| `UGGYGOAbilityGroupConfig` | 能力组并发规则 | 普攻组配 `SingleInstanceQueued`（连段靠它）；受击组配 `SingleInstance` + `bNewcomerWinsOnTie=false` |
| `UGGYGOAbilitySet` | 成组授予能力与效果 | 只填 Ability 与 GE。基础属性集（`HealthSet` / `CombatSet`）由 Slot 以默认子对象持有，**不再放进 `GrantedAttributes`**（见 [[计划_队伍与装配#9.3 D1 采纳的布局：Slot 持有 ASC 的三层结构|队伍与装配计划 9.3]]） |
| `UGGYGOPawnData` | 角色定义 | `PawnClass` 指向 `AGGYGOHeroCharacter` 子类；填 InputConfig / MovementSet / AbilityGroupConfig / AbilitySets / DefaultCameraMode |
| `UGGYGOCameraMode_ThirdPerson` 子类 | 默认镜头 | `TargetOffset` 调镜头距离与高度 |
| `UGGYGOExperienceDefinition` | 一局的玩法 | `SquadMembers` 是**默认编队**（玩家没编成时才用），按出场顺序填，上限 `GGYGO_MAX_SQUAD_SIZE` |
| 伤害 GE 蓝图 | 造成伤害 | Execution 选 `GGYGODamageExecution`；用 `SetByCaller.Damage` 传每段伤害 |
| 自毁 GE 蓝图 | 掉出世界 | 填到 `UGGYGOGameData::SelfDestructGameplayEffect`；个别角色要特殊行为时用 HealthComponent 的 `SelfDestructEffectOverride` |
| `UGGYGOGameData` | 项目级共享资产 | **必须建在 `/Game/System/GGYGOGameData`**（路径写在 `DefaultGame.ini` 里）。填伤害、治疗、自毁三个 GE |
| `UGGYGOGameplayCueNotify_HitImpact` 子类 | 命中特效 | `SurfaceEffects` 按表面 Tag 填粒子与音效，**更具体的表面排在前面**；`DefaultEffect` 兜底 |
| `UGGYGOBossDefinition` | Boss 的单 Form/单 Phase 最小定义 | `InitialFormTag` / `InitialPhaseTag` 各命中数组中的一项；Form 的 PawnData 指向 BossCharacter 子类；可填持久 AbilitySet 与 BehaviorTree |
| `AGGYGOBossEncounter` 蓝图或关卡实例 | 生成阶段 B Boss | 配 `BossDefinition`，放在出生位置；`bSpawnOnBeginPlay` 开启时服务器自动装配 State、Controller、Avatar |

**Cue 资产的放置位置**

Cue 资产必须放在 `Content/GameplayCues/` 下 —— `DefaultGame.ini` 的
`GameplayCueNotifyPaths` 只扫这个目录。放在别处不会报错，但所有 Cue 都会
找不到而静默不播，这是个很难排查的失败模式。

Cue 走按需异步加载，所以要在 PawnData 的 `CuesToPreload` 里列出该角色会用到的
Cue Tag 做预热，否则第一次触发时资产还没就位（"第一刀没有火花"）。
排查特效不播时可以用 `UGGYGOGameplayCueManager::DumpPreloadedCues` 确认
是加载问题还是 Cue 本身的问题。

**蓝图改动**

- 玩家角色蓝图父类改为 `AGGYGOHeroCharacter`，并在其 `HeroComponent` 上配 IMC
- `BP_PlayerController` 的父类已是 `AGGYGOPlayerController`（类名未变，无需改动）

**动画资产**

- 移动动画需要烘焙 `RootMotion_Speed` / `PosX` / `PosY` / `Dist` / `Yaw` / `DirX` / `DirY`
  与 `Cfg_ClipLength` / `Cfg_LoopTime` 曲线，由 `AAADocs/Scripts/` 的 Python 管线生成
  （逐帧值取自各动作 FBX 的外部 `Root` 节点，标量取自 AnimeStudio 导出的 per-clip JSON）
- 移动动画还需要经 `ZZZStripRootMotion` 做 in-place 化：原资产的 `Bip001` 轨道里
  带着与曲线同一份的整体位移与转身，不扣掉会转 360 度、位移走双倍
  （见 [[模块参考]] 4.5）。已处理 7 个 locomotion 动画，其余 112 个尚未处理
- 转身动画不需要 AnimNotify：相位边界由曲线判定
- 攻击 Montage 需要用 `AnimNotify_GameplayEvent`（或等价的自定义 Notify）发出这些事件，
  它们由 `PlayMontageAndWaitForEvent` 的 `EventReceived` 接收：

  | 事件 Tag | 攻击 GA 该做什么 |
  |---|---|
  | `Event.Montage.HitWindowBegin` | `MeleeTraceComponent->BeginTraceWindow()` |
  | `Event.Montage.HitWindowEnd` | `EndTraceWindow()` |
  | `Event.Montage.ComboWindow` | 此后按键可衔接下一段 |
  | `Event.Montage.CancelPoint` | 此后允许被其它动作打断 |

### 15.3 已知缺口

按影响程度排列。

**会让功能不完整**

1. **玩家攻击 GA 仍未实现；Boss 近战竖切已完成。** Boss 侧
   `UGGYGOBossMeleeAbility` 已串通 Montage、事件窗口、MeleeTrace、
   `GE_Damage_SetByCaller` 与命中 Cue，并在独立测试图中造成实际伤害。
   玩家侧 `GA_Attack_Light` / `GA_Dodge` / `GA_HitReact` / `GA_Death` 仍是空壳蓝图，
   只配了组身份与优先级，没有 Montage 编排。

2. **Boss 战斗 AI 已完成阶段 A–C，阶段 D–F 待实施。** 独立方案见 [[计划_BOSSAI]] 与
   [[GGYGO_流程_BossAI.canvas]]。当前已具备外置宿主、最小装配、ActionSet、确定性选招、
   BT 到 GAS 的激活桥和一条近战攻击竖切。单机 PIE 已确认 Montage Notify 驱动 Trace，
   命中后伤害把玩家生命从 `100` 降到 `0`。仍缺 Brain 暂停专项自动化、可见 Cue 表现、
   单次死亡与 Dedicated Server 验收；目标/仇恨、转阶段和换形态属于 D–E 阶段。

3. **动画重构已完成 Anim-A、Anim-B，并完成 Anim-C 的过渡拓扑回迁与重复状态清理。** 当前由
   `UGGYGOAnimInstanceBase` 在游戏线程集中从 CMC / ASC 抓取
   `FGGYGOAnimationStateFrame`；`ABP_Pyrios` 的七条逻辑 Transition 已直接组合
   `bHasMoveInput`、`IsAnimationRunGait()` 与 `IsTurnBackCurveDriven()`。
   `FZZZLocomotionDecisions`、七个状态名 UFUNCTION、`GaitValue` 与 `MovingSubState` 已删除。
   `UZZZAnimInstance` 仍把帧映射到旧 Snapshot，以兼容 `GaitBlendY`、`StopValue` 与调试字段；
   起步 0.5 秒早停窗口和 FName 资产查询仍待后续迁移。Editor/Game 构建与全蓝图编译均通过。
   CMC 也仍从 AnimInstance 读取 `RootMotion_*` 曲线，所以双向依赖尚未彻底解除。
   后续阶段见 [[计划_动画与表现层]]。

**联机相关**

4. **转身第一段在服务器不评估动画时会分歧。** 当前曲线值来自本地 AnimInstance；
   最终方案不优先传输实时 AnimGraph 曲线，而是让服务器与客户端按相同的
   MotionProfile Id、起始时刻和相位独立重演。实现阶段再决定哪些字段进入
   `FSavedMove` 与 `FCharacterNetworkMoveData`。
5. **联机未实测**。预测与纠正的正确性只做了静态推理，没有跑过 Dedicated Server。

**手感相关**

6. **松手防抖窗口未实现**。快速点按方向键会让动画在起步与停止之间抖动，
   抑制它需要输入层持有一个短的方向保持窗口。
7. **`IA_Sprint` / `IA_ForceWalk` 无效果**。步态只有走跑两档、靠持续时长升档，
   没有"按住冲刺"这一档，这两个键的语义待定。

**验证状态**：装配链路已在独立游戏里跑通 —— GameMode 建位置、生成 Pawn、
注入 ASC、附身出战角色这一串有日志确认，属性集正常挂载。
编队存档的写入、重启后读回、清空回落三条路径也各验证过一次。

尚未验证的是**联机**：所有运行都是单机独立游戏，客户端预测、
属性复制、Cue 在旁观者侧的表现都没有实测过。
手感相关的结论（步态、转身）仍只是静态检查与编译通过。

### 15.4 共享资产的归属

哪些 GameplayEffect 该放 `UGGYGOGameData`、哪些该配在别处，判断标准是
**谁施加它**。

| GE 类型 | 归属 | 理由 |
|---|---|---|
| 伤害、治疗 | `UGGYGOGameData` | 施加者可能是攻击能力、环境伤害、跌落伤害。放在能力上会让环境伤害拿不到 |
| 自毁 | `UGGYGOGameData` | 由 HealthComponent 直接应用，没有对应的能力 |
| 冷却 | **能力自己** | GAS 原生支持 `CooldownGameplayEffectClass`，每个能力的冷却时长本就不同 |
| 限制类（禁止移动、无敌帧） | **能力自己** | 它们属于施加它们的那个能力，随能力结束而移除 |
| 属性初始化 | `UGGYGOAbilitySet` | 每个角色的初始属性不同，属于角色定义 |

`UGGYGOAssetManager` 有两处刻意不用 `Fatal`（Lyra 原版用的是 Fatal）：

- **GameData 资产缺失**只警告。那份资产是在编辑器里创建的，若在启动时中止，
  引擎会在能创建资产之前就起不来 —— 一个死循环。
- **`AssetManagerClassName` 配错**用 Error 加兜底实例。这是个在编辑器里
  就能改好的配置问题，把工具链堵死代价太大。

因此 `UGGYGOGameData::Get()` 返回**可空指针**，调用方必须判空并给出
指向自身功能的错误信息。

---
