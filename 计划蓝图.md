# GGYGO 架构计划蓝图

> 本文是 GGYGO 以 Lyra 为参考的架构设计蓝图。分层记录每一层的**Lyra 事实**（读源码确认过的，不是印象）、**GGYGO 诉求**、**落地方案**和**待决策项**。
>
> 参考工程：`F:\ue_project\LyraStarterGame`（只读）。目标工程：`f:\ue_project\GGYGO`。
>
> 配套架构图：[[GGYGO_整体架构.canvas]]。图中节点颜色含义：绿=照抄 Lyra，青=GGYGO 自建或扩展，紫=现有代码保留，黄=UE/GAS 原生，橙=待改造或待拍板。

---

## 0. 三条硬约束

所有设计都被这三条倒推，任何方案先过这三关：

| 约束 | 来源 | 对架构的强制要求 |
|---|---|---|
| **动作游戏** | 核心玩法 | 帧级响应、输入缓冲、动作优先级与打断、命中判定窗口。GA 之间必须有可比较的优先级，不能只有二元的独占/非独占。 |
| **队伍多角色** | 原神/绝区零式，队伍多人、可切换、下场角色 AI 参战 | 每个角色需要**独立的属性、冷却、Buff**。这条直接否掉"一个玩家一个 ASC"的 Lyra 默认布局。 |
| **多人联机** | 使用 GAS 的根本原因 | 所有改变游戏状态的行为必须走 GAS 预测/复制链路。**现有 `FMotionDriver` 直接改 Actor 位置的做法在联机下不成立**，见第 10 章。 |

三条约束里，**多人联机是最强的**。它不是"以后再加"的特性——一旦有逻辑绕过了复制链路，后期改造等于重写。因此蓝图里每一层都要标注联机边界。

---

## 1. 分层总览

```
┌─ 输入层        Enhanced Input → InputTag                    [UE 输入框架 + Lyra 照抄]
├─ 意图层        InputTag/原始轴 → 语义意图                    [GGYGO 自建，Lyra 无]
├─ 仲裁层        意图 + 优先级 + 组规则 → 允许哪个动作          [GGYGO 扩展 Lyra 激活组]
├─ GAS 层        TryActivateAbility → GA/GE/Cue/Event         [Lyra 照抄 + GGYGO 扩展]
├─ 表现层        Montage / GameplayCue / 逻辑投影 → 动画特效    [GGYGO 自建接线]
└─ 移动层        Locomotion Pipeline + RootMotionSource        [现有代码改造]
```

层间铁律：

1. **上层只产出请求，不直接改状态。** 意图层产出"想攻击"，不产出"已攻击"。
2. **状态变更的唯一入口是 GAS。** 除移动输入外，任何改变角色状态的行为都通过 `TryActivateAbility` 或 `ApplyGameplayEffect`。
3. **表现层只读。** AnimBP、Cue、UI 只消费状态，不反向写权威数据。这条现有项目已经在守（`RuntimeData.ZZZAnim` 是单向投影）。
4. **移动层是唯一例外。** Locomotion 不走 GAS（和 Lyra 一致），但必须走 CharacterMovementComponent 以获得复制与预测。

---

## 2. AbilitySystem 层：照抄 Lyra 的部分

### 2.1 可以直接照抄的清单

| Lyra 文件 | GGYGO 对应位置 | 照抄程度 | 说明 |
|---|---|---|---|
| `AbilitySystem/LyraAbilitySystemComponent.*` | `AbilitySystem/GGYGOAbilitySystemComponent.*` | 照抄 + 扩展 | 输入 Tag 缓存、`ProcessAbilityInput`、TagRelationship 全部保留；激活组部分要重写，见第 5 章 |
| `AbilitySystem/LyraAbilitySet.*` | `AbilitySystem/GGYGOAbilitySet.*` | 直接照抄 | 成组授予 Ability/Effect/AttributeSet 并可整组回收，队伍换角色时这个能力是必需的 |
| `AbilitySystem/LyraAbilitySystemGlobals.*` | `AbilitySystem/GGYGOAbilitySystemGlobals.*` | 直接照抄 | 只为了让自定义 EffectContext 生效 |
| `AbilitySystem/LyraGameplayEffectContext.*` | `AbilitySystem/GGYGOGameplayEffectContext.*` | 直接照抄 | 携带 AbilitySource 与命中结果，伤害结算必需 |
| `AbilitySystem/LyraAbilitySourceInterface.*` | 同名迁移 | 直接照抄 | 配合上面的 EffectContext |
| `AbilitySystem/LyraAbilityTagRelationshipMapping.*` | 同名迁移 | 直接照抄 | Tag 级的 Block/Cancel 关系表，和第 5 章的组优先级是两套互补机制 |
| `AbilitySystem/Abilities/LyraGameplayAbility.*` | `AbilitySystem/Abilities/GGYGOGameplayAbility.*` | 照抄 + 扩展 | 保留 ActivationPolicy、AdditionalCosts、失败反馈、相机模式；扩展优先级与组，见第 5 章 |
| `AbilitySystem/Abilities/LyraAbilityCost*.*` | `AbilitySystem/Abilities/Costs/` | 照抄骨架 | Lyra 的三个实现绑定 Inventory，GGYGO 换成能量/耐力/弹药 |
| `AbilitySystem/Attributes/LyraAttributeSet.h` | `AbilitySystem/Attributes/GGYGOAttributeSet.h` | 直接照抄 | 基类 + `ATTRIBUTE_ACCESSORS` 宏 |
| `AbilitySystem/Attributes/LyraHealthSet.*` | `AbilitySystem/Attributes/GGYGOHealthSet.*` | 照抄 + 扩展 | Health/MaxHealth + Damage/Healing 元属性 + 死亡委托；动作游戏再加韧性/削韧 |
| `AbilitySystem/Attributes/LyraCombatSet.*` | `AbilitySystem/Attributes/GGYGOCombatSet.*` | 直接照抄 | BaseDamage/BaseHeal |
| `AbilitySystem/Executions/LyraDamageExecution.cpp` | `AbilitySystem/Executions/GGYGODamageExecution.*` | 照抄结构 | 距离衰减换成动作游戏的部位倍率/防御结算 |
| `AbilitySystem/LyraGameplayCueManager.*` | `AbilitySystem/Cues/GGYGOGameplayCueManager.*` | 直接照抄 | 只做异步/预加载优化，不含 Cue 执行逻辑 |
| `AbilitySystem/LyraGlobalAbilitySystem.*` | `AbilitySystem/GGYGOGlobalAbilitySystem.*` | 按需 | 向全场 ASC 广播 Ability/Effect（全局 Buff、区域效果）。**队伍架构下这个特别有用**：给全队上 Buff |
| `AbilitySystem/Phases/LyraGamePhase*` | `AbilitySystem/Phases/` | 暂缓 | 用 Ability 表达关卡阶段，GGYGO 现阶段没有关卡流程需求 |

### 2.2 现有代码的处置

| 现有文件 | 处置 |
|---|---|
| `Attributes/GGYGOAttributeSet.*` | 迁到 `AbilitySystem/Attributes/`，并按 Lyra 拆成 `GGYGOAttributeSet`(基类) + `GGYGOHealthSet` + `GGYGOCombatSet`。现有的 Health/MaxHealth/AttackPower/Defense/MoveSpeed/IncomingDamage 分散到对应 Set |
| `GGYGOGameplayEffects.h/.cpp` | 迁到 `AbilitySystem/Effects/`。当前是一组 soft class pointer + `InitGEGlobals()` 同步加载，没有失败回退。改造时接入 AssetManager 的异步加载 |
| `GGYGOTags.h` | 迁到 `System/GGYGOGameplayTags.h`。当前用 `namespace + static const FName` + `RequestGameplayTag` 运行时解析，Lyra 用 `UE_DEFINE_GAMEPLAY_TAG_STATIC` 原生 Tag 声明，后者是编译期注册、无字符串查找、可被 `.ini` 校验。**建议换成 Lyra 的写法**，`FGGYGOTagCache` 整个可以删掉 |
| `Pipeline/Arbiters/GASArbiter.*` | 保留但降级。它现在做的是"读 Restriction Tag → 写 `bBlockMove/Attack/Dodge`"。攻击/闪避改由 GA 的 Tag 需求（`ActivationBlockedTags`）自己判断，GASArbiter 只保留移动封锁这一项 |
| `Pipeline/Arbiters/ActionArbiter.*` | 删除。它的职责（动作优先级、状态抗性）被第 5 章的组优先级仲裁完整取代，且现在是空实现 |
| `Pipeline/Arbiters/HealthArbiter.*` | 删除。伤害应用改由 GE + `GGYGODamageExecution` 处理，现在的 `FDamageRequest` 队列是空转 |
| `Pipeline/Arbiters/StaminaArbiter.*` | 删除。耐力改成 AttributeSet 属性 + `GGYGOAbilityCost_Stamina` |
| `Pipeline/Intents/AttackIntentProcessor.*`<br>`Pipeline/Intents/DodgeIntentProcessor.*` | 保留但改语义。现在只写一个 bool 且无人消费；改为产出结构化意图并交给仲裁层，见第 4 章 |
| `StateMachine/` 整套 | **待决策**，见第 12 章 |

---

## 3. 输入层与意图层

### 3.1 Lyra 的事实（已读源码确认）

Lyra 的输入分成**两条完全独立的路**：

```
路 A（动作 → GAS）：
  按键 → UInputAction → ULyraInputConfig 查表 → FGameplayTag(InputTag.*)
       → ULyraInputComponent::BindAbilityActions
       → ULyraHeroComponent::Input_AbilityInputTagPressed(InputTag)    [LyraHeroComponent.cpp:343]
       → ULyraAbilitySystemComponent::AbilityInputTagPressed(InputTag) [:351]
       → 按 Tag 扫描 Spec，缓存 Handle
       → ProcessAbilityInput（每帧）→ TryActivateAbility

路 B（移动/视角 → 不走 GAS）：
  按键 → BindNativeAction(InputTag_Move / Look_Mouse / Look_Stick / Crouch / AutoRun)
       → ULyraHeroComponent::Input_Move / Input_LookMouse ...           [:285-289]
       → 直接 AddMovementInput / AddControllerYawInput
```

**关键结论：Lyra 的移动完全不经过 GAS。** 只有跳跃、开火、交互这类"动作"走 Ability。

另外两个细节值得抄：
- `AddAdditionalInputConfig` / `RemoveAdditionalInputConfig`：运行时增删输入配置。**队伍换角色时正好用它换掉整套角色技能键位**。
- `NAME_BindInputsNow`：通过 `UGameFrameworkComponentManager` 广播的扩展事件，GameFeature 插件可以在这个时机注入自己的输入绑定。

### 3.2 GGYGO 诉求

> Lyra 是直接的输入绑定 InputTag，然后根据 ability 绑定的 tag 找到 ability 激活。我认为这样在动作游戏里面并不是很好，还是需要一个类似于 input 转意图的意图管线。

### 3.3 我的意见：意图层要有，但位置和职责必须收紧

**同意加意图层**，理由是 Lyra 的 `InputTag → Ability` 是一对一硬映射，动作游戏里同一个按键的语义依赖上下文：

- 轻攻击键在地面是普攻、空中是空袭、闪避后是反击
- 长按 0.3 秒变蓄力，连按变连段
- 方向键 + 闪避 = 定向闪避，无方向 = 后跳
- 攻击键在受击硬直窗口内按下，要进输入缓冲而不是丢弃

这些都是"一个物理输入 → 多种语义动作"，用 InputTag 直连表达不了，会退化成在每个 GA 的 `CanActivateAbility` 里塞上下文判断，逻辑分散且无法做输入缓冲。

**但意图层必须严格限制在"解释"，不能碰"执行"。** 具体边界：

| 意图层**可以**做 | 意图层**不可以**做 |
|---|---|
| 读原始输入与输入历史（长按时长、连按次数、方向） | 决定角色实际执行什么动作 |
| 读只读的上下文（当前是否在空中、当前 ASC Tag） | 修改 ASC 状态、应用 GE、改变属性 |
| 维护输入缓冲队列与有效窗口 | 直接改 Actor 位置或播放动画 |
| 产出结构化意图 `FGGYGOActionIntent` | 跳过 GAS 自己"执行"这个意图 |
| 把意图翻译成一个候选 Ability 列表 | 决定最终哪个 Ability 胜出（那是仲裁层的事） |

**意图层的输出只有一件事：一个带优先级的候选动作请求，交给仲裁层，最终仍然通过 `TryActivateAbility` 进 GAS。**

这一点在联机下是硬要求。GAS 的客户端预测建立在"客户端调 `TryActivateAbility` → 生成 PredictionKey → 服务器验证"这条链上。如果意图层自己决定并执行了动作，就没有 PredictionKey，服务器无法验证，客户端也无法回滚。**意图层必须只跑在本地、只产出请求。**

### 3.4 落地结构

```
Input/
  GGYGOInputConfig.h/.cpp          照抄 Lyra，InputAction → InputTag 映射表
  GGYGOInputComponent.h/.cpp       照抄 Lyra，BindAbilityActions + BindNativeAction

Pipeline/Intents/                  （现有目录，改造）
  InputContextProbe.h/.cpp         只读采集上下文（空中/地面、ASC Tag、当前状态）
  ActionIntentResolver.h/.cpp      原始输入 + 上下文 → FGGYGOActionIntent
  InputBufferQueue.h/.cpp          输入缓冲：带时间窗的意图队列

Contracts/Intent/
  GGYGOActionIntent.h              意图结构体（纯数据）
```

意图结构草案：

```cpp
// Contracts/Intent/GGYGOActionIntent.h
USTRUCT()
struct FGGYGOActionIntent
{
    GENERATED_BODY()

    // 语义动作 Tag，如 Intent.Attack.Light / Intent.Attack.Charged / Intent.Dodge.Directional
    FGameplayTag IntentTag;

    // 意图产生的时刻，用于输入缓冲的有效窗判定
    float TimestampSeconds = 0.f;

    // 有效窗长度；超期未被消费则丢弃
    float BufferWindowSeconds = 0.f;

    // 方向意图（定向闪避、方向普攻），世界空间
    FVector WorldDirection = FVector::ZeroVector;

    // 蓄力/长按时长
    float HoldSeconds = 0.f;

    // 本次意图对应的候选 Ability 输入 Tag，交给仲裁层去 ASC 里找 Spec
    FGameplayTag AbilityInputTag;
};
```

**关键设计：意图层不持有 Ability 引用，只产出 `AbilityInputTag`。** 这样意图层不依赖 GAS 类型，可以保持纯 C++、可测试；同时保留了 Lyra 的 `AbilityInputTag → Spec` 查表机制，不重复造轮子。

### 3.5 移动输入怎么走

和 Lyra 一致：**移动/视角走 `BindNativeAction`，不进意图层，不进 GAS。** 现有 `FInputPipeline` + `LocomotionIntentProcessor` + `ViewRotationProcessor` 这条链保留，它们本来就在做这件事。意图层只处理"动作类"输入（攻击、闪避、技能、大招）。

---

## 4. GA 配置层：优先级 + 激活组 + 组规则

### 4.1 Lyra 的事实与局限

Lyra 有 `ELyraAbilityActivationGroup`，只有三个值：

```cpp
Independent            // 永不阻塞，也不被阻塞
Exclusive_Replaceable  // 可被其他 Exclusive 取消替换
Exclusive_Blocking     // 阻止所有其他 Exclusive 激活
```

判定实现（`LyraAbilitySystemComponent.cpp::IsActivationGroupBlocked`）是一个按枚举索引的固定数组 `ActivationGroupCounts[3]`，逻辑只有一句：**只要当前有任何 Blocking 能力，就拒绝所有新的 Exclusive**。

局限很明确：

1. **只有一个"独占"概念**，没有"组"。不能表达"技能之间互斥，但技能和普攻可以共存"。
2. **没有优先级数值**。`Exclusive_Replaceable` 之间是先到先得还是后到覆盖？实际是后到的无条件取消先到的——大招会被普攻打断。
3. **策略耦合在 GA 上**。想改"技能组只能同时一个"这条规则，得改每个技能 GA 的配置。

### 4.2 GGYGO 诉求

> 给 ga 的配置上再加一层比如技能优先级 level，然后 ga 也有激活组，就可以对比 ga 的优先级决定谁推出谁激活。做个类似于 ga 组的东西，然后封装规则，比如同组的 ga，这个是技能组，因为技能只能同时激活一个，所以这个组的规则就有同一组的 ga 激活的时候只保留一个。然后 ga 也可以在上面封装自己的激活规则，我目前想得就是独占，共存。

### 4.3 方案：三个正交维度

诉求里"组规则"和"GA 自己的激活规则"是两个不同层级的东西，把它们拆成三个正交维度，避免配置互相打架：

| 维度 | 配在哪 | 类型 | 职责 |
|---|---|---|---|
| **组身份** `GroupTag` | GA 资产 | `FGameplayTag` | 这个 GA 属于哪个组。如 `AbilityGroup.Attack` / `.Skill` / `.Ultimate` / `.Dodge` / `.HitReact` |
| **组规则** `GroupRule` | 独立 DataAsset | 枚举 + 参数 | 该组内部的并发规则。**配在组上，不配在 GA 上**——改规则只改一处 |
| **自身策略** `SelfPolicy` + `Priority` | GA 资产 | 枚举 + int32 | 这个 GA 在组规则之上的个体行为，以及冲突时的强弱 |

**组规则**（`EGGYGOAbilityGroupRule`）：

| 值 | 语义 | 典型用途 |
|---|---|---|
| `Coexist` | 组内任意多个可同时激活 | Buff 类、被动 |
| `SingleInstance` | 组内同时只能一个，按优先级决定去留 | 技能组、大招组 |
| `SingleInstanceQueued` | 组内同时只能一个，新请求进缓冲队列而不是取消旧的 | 普攻连段（等前一段结束再出下一段） |

**自身策略**（`EGGYGOAbilitySelfPolicy`）:

| 值 | 语义 |
|---|---|
| `Coexist` | 不主动排斥任何人，只受组规则约束 |
| `Exclusive` | 激活期间排斥**所有**组的低优先级 GA（不只是同组）。用于大招、死亡、被击倒 |

**优先级** `Priority`（int32，越大越强）：跨组和同组冲突都用它比较。建议分段留空隙便于插值：

```
死亡 / 强制状态       1000
被击倒 / 强硬直        800
大招                  600
技能                  400
闪避                  300
重攻击                200
轻攻击                100
被动 / Buff             0
```

### 4.4 仲裁算法

在 `UGGYGOAbilitySystemComponent` 里把 Lyra 的固定数组换成按组 Tag 索引的实例表：

```cpp
// 取代 Lyra 的 ActivationGroupCounts[3]
TMap<FGameplayTag, TArray<TWeakObjectPtr<UGGYGOGameplayAbility>>> ActiveAbilitiesByGroup;
```

`CanActivateAbility` 阶段（只判断，不改状态）：

1. 取请求 GA 的 `GroupTag` / `Priority` / `SelfPolicy`
2. **全局排斥检查**：遍历所有已激活 GA，若存在 `SelfPolicy == Exclusive` 且 `Priority >= 请求者Priority` 的实例 → 拒绝
3. **同组规则检查**：查 `GroupTag` 对应的 `GroupRule`
   - `Coexist` → 通过
   - `SingleInstance` → 若组内已有实例且其 `Priority >= 请求者Priority` → 拒绝；否则通过（标记待取消列表）
   - `SingleInstanceQueued` → 若组内已有实例 → 拒绝激活，但把意图压回输入缓冲队列
4. **Tag 关系检查**：交给照抄来的 `GGYGOAbilityTagRelationshipMapping`，处理与组无关的 Tag 级 Block/Cancel

`NotifyAbilityActivated` 阶段（改状态）：

5. 把新实例登记进 `ActiveAbilitiesByGroup`
6. 执行第 3 步标记的取消列表（`CancelAbilityHandle`）
7. 若 `SelfPolicy == Exclusive`，取消所有 `Priority <` 自己的其他组实例

`OnAbilityEnded` 阶段：

8. 从 `ActiveAbilitiesByGroup` 摘除
9. 若该组规则是 `SingleInstanceQueued`，通知输入缓冲队列尝试出队

**优先级相等时的处理必须明确**：上面用的是 `>=`，即**同优先级时后来者被拒绝**（先到先得）。这对连段是对的（第二段不打断第一段），对"同优先级技能互相打断"是错的。如果需要后者，改成 `>`。**这条建议做成组规则的一个参数**，而不是全局定死。

### 4.5 落地结构

```
AbilitySystem/
  GGYGOAbilitySystemComponent.h/.cpp     组表 + 仲裁实现
  Abilities/
    GGYGOGameplayAbility.h/.cpp          GroupTag / Priority / SelfPolicy 字段
  Groups/                                （新目录）
    GGYGOAbilityGroupConfig.h/.cpp       DataAsset：Map<GroupTag, FGGYGOAbilityGroupRule>
    GGYGOAbilityGroupTypes.h             枚举 + 规则结构体
```

---

## 5. GE 层

诉求里说"还没想到什么可以照抄 Lyra 的"。实际上有三个值得抄：

1. **`LyraGameplayEffectContext`**：自定义 EffectContext，携带 `AbilitySource` 和命中的物理材质。动作游戏的"打到金属出火花、打到肉出血"就靠这个。已列进第 2.1 节。
2. **`LyraDamageExecution`**：伤害结算用 `ExecutionCalculation` 而不是简单 Modifier。它的价值是能同时读源和目标的属性（攻击力 vs 防御力）、能读 EffectContext（命中部位）、能一次算完再写回。动作游戏的伤害公式必然需要这个。
3. **元属性模式**（`LyraHealthSet` 的 `Damage`/`Healing`）：GE 改的是 `Damage` 元属性，`PostGameplayEffectExecute` 里把它转换成 `Health` 扣减并清零。好处是伤害可以被拦截、修正、触发事件，而不是直接扣血。**现有 `GGYGOAttributeSet::HandleIncomingDamage` 已经是这个模式了**，保留并按 Lyra 补齐死亡委托。

GGYGO 需要自建的 GE 分类（放 `AbilitySystem/Effects/`）：

| 分类 | 用途 |
|---|---|
| 硬直 / 受击 | 施加 `Restriction.*` Tag + 持续时间，动作游戏的核心 |
| 削韧 / 破防 | 韧性属性的消耗与恢复 |
| 冷却 | 现有 `GGYGOGEs::Cooldown::*` 已有骨架 |
| 限制 | 现有 `GGYGOGEs::Restriction::*` 已有骨架 |
| 无敌帧 | 闪避的 i-frame，`Restriction.ImmuneDamage` |

---

## 6. GameplayEvent 层

诉问里说可以直接照抄，同意。GAS 原生的 `SendGameplayEventToActor` + `AbilityTask_WaitGameplayEvent` + `FGameplayEventData` 已经够用，Lyra 没有做额外封装。

动作游戏要额外接的两处：

1. **AnimNotify → GameplayEvent**：动画帧上的判定窗口、连段窗口、可取消点，用 AnimNotify 发 GameplayEvent，GA 内用 `WaitGameplayEvent` 接。这是动作游戏最常用的模式。**现有项目已有 `AnimNotify_CanYaw` 的雏形**，但它走的是 `AnimInstance → RuntimeComponent → Pipeline` 的直连转发，不是 GameplayEvent。新的动作类 Notify 建议直接走 GameplayEvent。
2. **命中 → GameplayEvent**：命中判定组件检测到命中后发事件，GA 决定要不要应用伤害 GE。

另外 `GameplayMessageRuntime`（已在 `GGYGO.Build.cs` 声明）和 GameplayEvent 是两个不同东西，别混：

| | GameplayEvent | GameplayMessage |
|---|---|---|
| 目标 | 特定 Actor 的 ASC | 全局广播，无目标 |
| 用途 | 驱动 Ability 逻辑 | 通知 UI / 音频 / 成就等旁观者 |
| 载荷 | `FGameplayEventData`（固定结构） | 任意 USTRUCT |

---

## 7. 表现层：GA 激活后怎么播动画和特效

### 7.1 事实澄清

诉求里说"lyra 里面没有 ga 表现类的或者说我还没看到"。确认一下：**Lyra 确实没有专门的"GA 表现类"**，它的表现分散在三处：

1. GA 内部直接用 `AbilityTask_PlayMontageAndWait` 播 Montage
2. GE/GA 触发 `GameplayCue.*` Tag，由 `GameplayCueNotify` 处理特效音效
3. `LyraGameplayCueManager` 只负责 Cue 资源的异步/预加载优化，**不含任何 Cue 执行逻辑**，执行全在 GAS 原生

所以"GameplayCue"这个猜测是对的，它就是 GAS 的表现层机制。

### 7.2 三条表现通道

| 通道 | 机制 | 适用 | 联机行为 |
|---|---|---|---|
| **A. Montage** | GA 内 `PlayMontageAndWait` | 有独立完整动画的动作：攻击、闪避、技能 | Montage 由 GAS 复制（`NetExecutionPolicy` 决定），本地预测播放 |
| **B. GameplayCue** | Tag 触发 `GameplayCueNotify_Static`（一次性）/ `GameplayCueNotify_Actor`（持续） | 特效、音效、屏幕震动、命中反馈 | Cue 自带复制与本地预测，且**服务器不执行**（`bLocalOnly` 语义清晰） |
| **C. 逻辑投影** | GA 写 ASC Tag / RuntimeData → AnimInstance 读快照 → AnimBP 状态机 | 需要和移动混合的表现：受击姿态、武器待机姿态、上下半身分层 | 依赖被投影数据本身的复制 |

### 7.3 与现有 ZZZAnim 的关系

现有项目是纯通道 C：`Pipeline → RuntimeData.ZZZAnim → UZZZAnimInstance 快照 → AnimBP`。这套要保留，因为 locomotion（ZZZ 的 RootMotion 曲线驱动、TurnBack）必须走它。

新增的动作类 GA 用通道 A + B：

```
GA_Attack_Light 激活
  ├─ 通道 A：PlayMontageAndWait(蒙太奇)              → 动作本体
  ├─ 通道 B：Montage 上的 AnimNotify 触发 GameplayCue → 刀光、音效
  ├─ 通道 B：命中时 GE 触发 GameplayCue.Hit.*        → 命中特效、震屏
  └─ 通道 C：给 ASC 加 State.Attacking Tag          → ZZZAnim 快照读到，AnimBP 切上半身层
```

**需要明确的边界：Montage 和 ZZZ 曲线位移会打架。** 攻击 Montage 自带位移（前冲），而 ZZZAnim 的 RootMotion 曲线也在算位移。两者必须二选一，或者由 GA 在激活期间暂时关闭 Pipeline 的曲线位移提交。这一条要在实现攻击 GA 时定，见第 12 章待决策项。

### 7.4 需要搭的框架

```
AbilitySystem/
  Cues/
    GGYGOGameplayCueManager.h/.cpp          照抄 Lyra 的异步加载优化
    GGYGOGameplayCueNotify_Burst.h/.cpp     一次性 Cue 基类（命中、格挡、暴击）
    GGYGOGameplayCueNotify_Looping.h/.cpp   持续 Cue 基类（燃烧、蓄力光效）
  Tasks/
    AbilityTask_PlayMontageAndWaitForEvent.h/.cpp
        动作游戏必需：播 Montage 的同时等 AnimNotify 事件。
        GAS 官方示例（ActionRPG）有，Lyra 没有，需要移植。
    AbilityTask_WaitForHit.h/.cpp
        等命中判定组件的命中事件
Contracts/Animation/
  ZZZAnimRuntimeModel.h                     （现有）增加 GA 状态投影字段
```

先建空骨架，具体 Cue 资产以后按需填充。

---

## 8. 整体架构：Owner / Avatar 与队伍多角色

这是**整个蓝图最需要先定死的一章**，它决定 ASC 放哪，而 ASC 放哪决定后面所有代码怎么写。

### 8.1 Lyra 的事实

Lyra 有两套布局：

**布局 1（玩家角色，默认）：ASC 挂 PlayerState**

```
ALyraPlayerState                     ← ASC 的 Owner
  ├─ ULyraAbilitySystemComponent     ← ASC 本体（LyraPlayerState.h:153）
  ├─ ULyraHealthSet / CombatSet      ← AttributeSet
  └─ PawnData (Replicated)           ← 角色定义
        │
        │ InitAbilityActorInfo(Owner=PlayerState, Avatar=Pawn)
        ▼
ALyraCharacter                       ← Avatar，GetAbilitySystemComponent() 转发到 PlayerState
  ├─ ULyraPawnExtensionComponent     ← 协调初始化顺序（InitState 状态机）
  ├─ ULyraHeroComponent              ← 输入绑定、相机
  └─ ULyraHealthComponent            ← 把 HealthSet 包成组件 API
```

Owner/Avatar 分离的价值：Pawn 死亡销毁重生时，ASC 和属性留在 PlayerState 上不丢，Ability 的冷却、Buff 全部延续。

**布局 2（AI/NPC）：ASC 挂 Pawn 自身**

`ALyraCharacterWithAbilities` 自己持有 ASC + HealthSet + CombatSet，`PostInitializeComponents` 里自行 `InitAbilityActorInfo(this, this)`。

### 8.2 队伍多角色的冲突

诉求：

> 我的游戏未来的需求就是我的角色可以像原神绝区零那样一个队伍有多个角色，我可以随意切换，甚至队伍里的角色同时上场战斗其他的 ai 战斗之类的。

这和 Lyra 布局 1 **直接冲突**。核心矛盾：

一个 ASC 只能挂**一套** AttributeSet（同类型只能一个实例）。如果三个角色共用 PlayerState 上的一个 ASC，那三个角色共享同一份 HP、同一份技能冷却——这不是绝区零，这是"换皮不换数据"。

绝区零/原神的实际模型是：**每个角色有独立的 HP、能量、技能冷却、Buff，下场后继续走冷却和回能**。这要求每个角色一套独立的属性和 Ability 状态。

### 8.3 三个候选方案

**方案 A：每角色一个 ASC 挂在 Pawn 上（类似 `LyraCharacterWithAbilities`）**

```
ALyraPlayerState 等价物              ← 只放玩家级/队伍级数据
  └─ 队伍资源、共享能量、羁绊

APlayerController
  └─ possess 切换 →  ┌─ GGYGOCharacter_A  [ASC_A + AttrSet_A]  ← 上场
                     ├─ GGYGOCharacter_B  [ASC_B + AttrSet_B]  ← 下场，常驻，CD 继续走
                     └─ GGYGOCharacter_C  [ASC_C + AttrSet_C]  ← 下场，AI 参战
```

- 优点：各角色状态天然独立；下场角色的 GE/CD/Buff 继续正常运转；AI 参战直接可行（下场角色由 AIController possess）；和 `LyraCharacterWithAbilities` 同构，有现成参考
- 缺点：3 个 ASC 都要复制（网络开销 ×3）；切换角色时输入绑定要重新指向新 ASC；玩家级数据要单独一套容器
- 联机：可行。每个 Pawn 独立复制，标准做法

**方案 B：ASC 挂 PlayerState（Lyra 原样），角色属性运行时切换**

切换角色时把当前角色的属性存进结构体，把新角色的属性写进 ASC 的单套 AttributeSet。

- 优点：只有一个 ASC，网络开销小；输入绑定不用重新指向
- 缺点：**下场角色的 GE 无法继续生效**（Buff、DoT、CD 都挂在 ASC 上，不属于某个角色），需要手动序列化/反序列化 ActiveGameplayEffects——这是在和 GAS 对抗；下场角色 AI 参战做不到（没有自己的 ASC 就没法激活 Ability）
- 结论：**不推荐**。它省的那点网络开销，要用大量违背 GAS 设计的手工状态搬运来换

**方案 C（推荐）：每角色一个 ASC 挂 Pawn + 玩家级 ASC 挂 PlayerState**

```
AGGYGOPlayerState                    ← 玩家/队伍级 ASC 的 Owner
  ├─ UGGYGOAbilitySystemComponent    ← 队伍 ASC
  ├─ UGGYGOTeamAttributeSet          ← 队伍共享资源（切人能量、连携值）
  └─ 队伍 Ability（切人技、连携技、队伍 Buff）

每个角色 Pawn（三个都常驻在世界里）
  ├─ UGGYGOAbilitySystemComponent    ← 角色 ASC（Owner=自己，Avatar=自己）
  ├─ UGGYGOHealthSet / CombatSet     ← 角色独立属性
  └─ UGGYGOPawnExtensionComponent    ← 照抄 Lyra 的 InitState 协调

上场角色额外挂：
  └─ UGGYGOHeroComponent             ← 输入绑定、相机；切换时在角色间转移
```

- 优点：兼顾角色独立性和队伍级共享数据；切人技/连携技有明确归属（队伍 ASC）；下场角色可由 AIController 接管参战；全场 Buff 用照抄来的 `GGYGOGlobalAbilitySystem` 广播
- 缺点：两级 ASC，跨级查询要写清楚（角色 Ability 读队伍资源要拿 PlayerState 的 ASC）
- 联机：可行，是这三个方案里最贴合需求的

### 8.4 切换角色的流程（按方案 C）

```
1. 输入触发切人 → 队伍 ASC 的 GA_SwitchCharacter（走 GAS，可被打断、有 CD、消耗切人能量）
2. GA 内：旧角色 UGGYGOHeroComponent 解绑输入（RemoveAdditionalInputConfig）
3. Controller UnPossess 旧角色 → Possess 新角色
4. 新角色 HeroComponent 绑定输入（AddAdditionalInputConfig，换成新角色技能键位）
5. 相机目标切换（CameraComponent 跟随新 Avatar）
6. 旧角色交给 AIController（如果要"同时上场战斗"）或进入待机（隐藏但 ASC 继续运转）
7. 全程走 GAS：服务器权威 Possess，客户端预测表现
```

**注意第 3 步在联机下必须服务器权威。** `Possess` 只能在服务器执行，客户端只能预测表现层（相机、UI），不能预测控制权转移。

---

## 9. 联机与现有 Pipeline 的冲突

### 9.1 问题

现有 `FMotionDriver` 直接调用 Actor 位移 API 提交移动，绕过 `UCharacterMovementComponent`。位移来源是 ZZZ 动画曲线（`RM_PosX` / `RM_PosY` / `RM_Yaw`）的采样差分。

这在联机下不成立：

1. **没有移动复制**。CMC 的 `ReplicateMoveToServer` / `ClientAdjustPosition` 全部被绕过，服务器和客户端各算各的，必然漂移。
2. **没有客户端预测与纠正**。玩家在高延迟下会看到角色橡皮筋。
3. **服务器无法验证**。作弊客户端可以任意改位置。

### 9.2 三个选项

| 选项 | 做法 | 代价 | 联机可用 |
|---|---|---|---|
| **1. 改造成 RootMotionSource（推荐）** | 曲线位移不再直接写 Actor，而是构造 `FRootMotionSource_*` 交给 CMC，由 CMC 负责应用、复制、预测、纠正 | 重写 `FMotionDriver` 的提交层；曲线采样逻辑可以完整保留 | 是 |
| **2. 用 GAS 的 RootMotion AbilityTask** | `AbilityTask_ApplyRootMotionConstantForce` 等，本质也是 RootMotionSource，但由 GA 驱动 | 位移必须由 GA 发起，locomotion 也要变成 Ability——和 Lyra"移动不走 GAS"的结论相悖 | 是 |
| **3. 保持现状，先做单机** | 不动 | 联机时必须重写，且届时 Pipeline 已经和更多系统耦合，改动面更大 | 否 |

**我的建议是选项 1**，但**不是现在做**。理由：RootMotionSource 改造的前提是先把 GAS 层立起来（因为攻击/闪避的位移也要走同一套机制），现在改会返工两次。建议顺序是：GAS 核心 → 攻击/闪避 GA（这时就用 RootMotionSource 写位移）→ 回头把 locomotion 的曲线位移也迁过去。

**但这条必须现在就承认并记录**，不能等到联机时才发现。这也是为什么这一章放在实施顺序之前。

---

## 10. GameFeature 架构

### 10.1 Lyra 的模型

```
LyraExperienceDefinition (DataAsset)
  ├─ DefaultPawnData                  ← 默认角色
  ├─ GameFeaturesToEnable[]           ← 要激活的 GameFeature 插件
  └─ Actions[]                        ← GameFeatureAction 列表
        ├─ AddComponents              ← 给指定 Actor 类加组件
        ├─ AddAbilities               ← 给 ASC 加 AbilitySet
        ├─ AddInputContextMapping     ← 加输入映射
        └─ AddWidgets                 ← 加 UI

LyraExperienceManagerComponent（挂 GameState）
  └─ 加载 Experience → 激活 GameFeature → 执行 Actions → 广播就绪
```

关键点：**GameFeature 的价值是"按需装配"，代价是"初始化时序变复杂"**。Lyra 用 `UGameFrameworkComponentManager` 的 InitState 状态机（`ULyraPawnExtensionComponent`）来协调"组件什么时候能用"。这套时序是 Lyra 最难懂的部分，也是照抄时最容易出问题的地方。

### 10.2 对 GGYGO 的建议

**同意用 GameFeature，但建议分两步走，不要一开始就上。**

第一步（现在）：**只抄 InitState 协调机制，不抄 GameFeature 插件化**

- 抄 `LyraPawnExtensionComponent` 的 InitState 状态机，用来协调 ASC / PawnData / HeroComponent 的初始化顺序
- 抄 `LyraHeroComponent` 的 `NAME_BindInputsNow` 扩展点
- 不建 GameFeature 插件，Ability 和输入直接由 PawnData + AbilitySet 静态配置

理由：InitState 机制本身就有独立价值（解决"ASC 还没好，HeroComponent 就想绑输入"这类竞态），而且它是 GameFeature 的前置。现有项目的 `BeginPlay` 里已经有一条脆弱的手工顺序（`InitGEGlobals` → `InitAbilityActorInfo` → Pipeline 初始化 → `StateManager::Init` → `InitASC`），正好用 InitState 替换。

第二步（角色数量上来之后）：**再把角色做成 GameFeature 插件**

- 每个角色一个 GameFeature 插件（角色 Ability、动画资产、输入配置、Cue）
- 队伍配置决定激活哪些插件
- 这时 GameFeature 的"按需加载"才有实际收益（不用把全部角色资产常驻内存）

如果现在就上 GameFeature，收益是零（只有几个角色），成本是要同时调试 GameFeature 时序 + GAS 时序 + 现有 Pipeline 时序三套东西。

### 10.3 落地结构

```
GameModes/
  GGYGOExperienceDefinition.h/.cpp        第二步再建
  GGYGOExperienceManagerComponent.h/.cpp  第二步再建
Character/
  Components/
    GGYGOPawnExtensionComponent.h/.cpp    第一步就建，InitState 协调
    GGYGOHeroComponent.h/.cpp             第一步就建，输入 + 相机
    GGYGOHealthComponent.h/.cpp           第一步就建，HealthSet 的组件 API
  Data/
    GGYGOPawnData.h/.cpp                  第一步就建，角色定义 DataAsset
```

---

## 11. 实施顺序

每一阶段结束都要能编译、能跑、能提交。

| 阶段 | 内容 | 产出验证 |
|---|---|---|
| **1. GAS 核心层** | 照抄第 2.1 节清单的"直接照抄"项：ASC、AbilitySet、Globals、EffectContext、AbilitySourceInterface、TagRelationshipMapping、GameplayAbility 基类、AttributeSet 三件套 | 编译通过；角色能被授予一个空 GA 并激活 |
| **2. Tag 体系重写** | `GGYGOTags.h` 换成 `UE_DEFINE_GAMEPLAY_TAG_STATIC` 原生声明，删 `FGGYGOTagCache`，补 `AbilityGroup.*` / `Intent.*` / `GameplayCue.*` 分类 | 编辑器 Tag 列表正确；GASArbiter 改用原生 Tag |
| **3. 组优先级仲裁** | 第 4 章的三维度模型 + `ActiveAbilitiesByGroup` + 仲裁算法 + GroupConfig DataAsset | 两个不同优先级的 GA 互相打断行为符合预期 |
| **4. 组件化与 InitState** | 第 10.2 节第一步：PawnExtension / Hero / Health / PawnData，替换现有 `BeginPlay` 手工顺序 | 初始化顺序不再依赖手工排列；Pipeline 仍正常工作 |
| **5. 输入与意图层** | InputConfig / InputComponent 照抄 + 意图层三个文件 + 输入缓冲 | 长按/连按/定向能产出正确意图；意图能激活对应 GA |
| **6. 表现层框架** | CueManager、两个 CueNotify 基类、`PlayMontageAndWaitForEvent` AbilityTask | 一个攻击 GA 能播 Montage 并触发一个 Cue |
| **7. 第一个完整动作** | GA_Attack_Light：Montage + 命中判定 + 伤害 GE + Cue + 硬直，位移用 RootMotionSource | 打得出来、打得到、有反馈、能被打断 |
| **8. locomotion 迁 RootMotionSource** | 第 9.2 节选项 1，把现有曲线位移改成 CMC RootMotionSource | 移动手感不变；`FMotionDriver` 不再直接写 Actor 位置 |
| **9. 队伍多角色** | 第 8.3 节方案 C：两级 ASC + 切人 GA + HeroComponent 转移 | 三个角色可切换，各自 HP/CD 独立，下场角色 CD 继续走 |
| **10. 联机** | Dedicated Server 起服，验证预测与纠正 | 两个客户端看到一致的战斗表现 |
| **11. GameFeature 插件化** | 第 10.2 节第二步 | 角色资产按需加载 |

---

## 12. 待决策清单

按对架构的影响排序。前三项不定，第 1 阶段就没法开工。

| # | 问题 | 选项 | 影响面 |
|---|---|---|---|
| **1** | **ASC 放哪** | A 每角色 Pawn 一个 / B PlayerState 一个 / **C 两级（推荐）** | 决定所有 GAS 代码的 Owner-Avatar 假设。定错要全改 |
| **2** | **联机形态** | 每个玩家各带一支队伍 / 每个玩家控制一个角色（队伍是单机概念）；Listen Server / Dedicated Server | 决定复制策略、ASC 数量、队伍数据归属 |
| **3** | **现有 locomotion Pipeline 的定位** | 保留做 locomotion 并改造成 RootMotionSource（推荐）/ 整体废弃改用 CMC 原生 + GAS | 决定 `FMotionDriver`、`FGaitAuthorityProcessor`、TurnBack 那套曲线逻辑是留是弃 |
| **4** | **`StateMachine/` 整套的去留** | 保留做 locomotion 状态（Idle/Moving）/ 完全由 GAS Tag + AbilityGroup 取代 | 现有 `FGYGOStateManager` + Idle/Moving State 有实际逻辑，但 GAS 的 Tag 状态和它职责重叠。**倾向：降级为纯 locomotion 状态机，动作状态全部交给 GAS** |
| **5** | **同优先级 GA 的默认行为** | 先到先得（`>=`，推荐做默认）/ 后来者覆盖（`>`） | 影响连段和同级技能互断。建议做成组规则参数 |
| **6** | **攻击 Montage 位移 vs ZZZ 曲线位移** | GA 激活期间暂停 Pipeline 曲线提交 / 两者都转成 RootMotionSource 由 CMC 叠加 | 影响第 7 阶段。倾向后者 |
| **7** | **是否需要 `GGYGOGlobalAbilitySystem`** | 需要（队伍 Buff、区域效果）/ 暂缓 | 影响第 1 阶段的照抄范围 |

---

## 13. 与 Lyra 的差异总表

一眼看清哪些是照抄、哪些是自己的东西。

| 层 | Lyra 做法 | GGYGO 做法 | 性质 |
|---|---|---|---|
| ASC 归属 | PlayerState 单个 | 每角色 Pawn 一个 + PlayerState 队伍级一个 | **重大改动** |
| 输入 → 动作 | InputTag 直连 Ability | InputTag → 意图层 → 仲裁 → Ability | **新增一层** |
| 移动输入 | `BindNativeAction`，不走 GAS | 同 Lyra，不走 GAS | 一致 |
| GA 并发控制 | 三值枚举 + 单组计数 | 组 Tag + 组规则 DataAsset + int32 优先级 + 自身策略 | **重大扩展** |
| Tag 关系 | `AbilityTagRelationshipMapping` | 照抄，与组优先级并存互补 | 照抄 |
| 成本 | `LyraAbilityCost` 可插拔 | 照抄，换成能量/耐力 | 照抄 |
| 伤害 | `DamageExecution` + 元属性 | 照抄，公式换动作游戏的 | 照抄 |
| 表现 | Montage + GameplayCue | 同 Lyra，额外接现有 ZZZAnim 投影通道 | 照抄 + 新增通道 |
| 位移 | CMC 原生 + RootMotionSource | 现有曲线驱动改造成 RootMotionSource | **改造现有** |
| 组件初始化 | InitState 状态机 | 照抄 | 照抄 |
| 数据装配 | Experience + GameFeature | 分两步，先静态 PawnData，后 GameFeature | 照抄但延后 |
| 队伍切换 | 无此概念 | 队伍 ASC 的切人 GA + HeroComponent 转移 | **全新** |

---

## 附录 A：原始诉求原文

保留本文优化前的手写原文，作为需求出处，后续设计如与下文冲突，以上文经确认的决策为准。

> 首先就是 abilitysystem，这个我认为基本可以照抄 lyra
>
> 然后输入方面，这方面我需要你和我一起考虑一下，就是 lyra 是直接的输入绑定 inputtag，然后根据 ability 的绑定的 tag 来找到 ability 激活的，但是我认为这样在动作游戏里面并不是很好，我认为还是需要一个类似于 input 转意图这样的意图管线，你可以参考一下我的意见，说出你的想法
>
> ga 配置方面，目前 lyra 除了 gas 自己的 ga 配置，他给 ga 配置了个激活组，我也会有类似的配置，但是我的需要改进的是因为我的这个是动作游戏所以不同技能 ga 之间是有优先级的，所以我打算给 ga 的配置上再加一层比如技能优先级 level，然后 ga 也有几乎组，然后就可以对比 ga 的优先级决定谁推出谁激活了，然后我认为可以做个类似于 ga 组的东西，然后封装规则，比如同组的 ga，比如这个是技能组，因为技能只能同时激活一个，所以这个组的规则就有同一组的 ga 激活的时候只保留一个，然后 ga 也可以在上面封装自己的激活规则，我目前想得就是独占，共存，这个你可以先计划着
>
> ge 因为我还没想到什么可以照抄 lyra 的，以及 gameevent 也可以直接照抄
>
> 关于表现，因为 lyra 里面没有 ga 表现类的或者说我还没看到所以我不知道怎么做，就是怎么这个 ga 激活之后 animinstance 去播放这个 ga 对应的动画，甚至还有例子特效那些，我听说是有个 gamecue，这个你先把框架搭好，以后我用到的时候直接填充
>
> 然后就是整体架构，他是逻辑和表现分层，也就是 owner 和 avatar 分开吧，我也可以这样做，我的游戏未来的需求就是我的角色可以像原神绝区零那样一个队伍有多个角色，我可以随意切换，甚至队伍里的角色同时上场战斗其他的 ai 战斗之类的，这个你需要考虑到
>
> 我的这个游戏计划的可以多人联机的游戏，这就是我是用 gas 的原因
>
> 我希望采用 gamefeature 架构

### 原文诉求到本文章节的对应

| 原文诉求 | 本文处理 | 结论摘要 |
|---|---|---|
| abilitysystem 照抄 lyra | 第 2 章 | 列出 16 项可照抄清单，其中 ASC 与 GameplayAbility 基类需扩展 |
| 输入转意图管线，要我的想法 | 第 3 章 | 同意加意图层，但严格限制在"解释"，输出 `FGGYGOActionIntent` + `AbilityInputTag`，最终仍走 `TryActivateAbility` 以保住 GAS 预测 |
| ga 加优先级 level + 激活组 + 组规则 + 独占/共存 | 第 4 章 | 拆成三个正交维度：组身份 `GroupTag`（GA 上）、组规则（独立 DataAsset）、自身策略 + `Priority`（GA 上）；给出四阶段仲裁算法 |
| ge 没想到可抄的 | 第 5 章 | 实际有三项值得抄：自定义 EffectContext、DamageExecution、元属性模式 |
| gameevent 直接照抄 | 第 6 章 | 同意；补充 AnimNotify → GameplayEvent 用法，并区分 GameplayEvent 与 GameplayMessage |
| ga 表现（动画/特效），听说有 gamecue，先搭框架 | 第 7 章 | 确认 Lyra 没有专门的 GA 表现类；给出三条通道（Montage / GameplayCue / 逻辑投影）与框架文件清单 |
| owner/avatar 分层 + 队伍多角色可切换 + 下场 AI 参战 | 第 8 章 | Lyra 默认 PlayerState 单 ASC 在此需求下不成立；给三方案并推荐方案 C 两级 ASC |
| 多人联机（用 GAS 的原因） | 第 9 章 | 指出现有 `FMotionDriver` 绕过 CMC 在联机下不成立，须改为 RootMotionSource |
| gamefeature 架构 | 第 10 章 | 同意但建议分两步：先只抄 InitState 协调机制，角色数量上来后再插件化 |
