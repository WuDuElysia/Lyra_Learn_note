# GGYGO 架构计划蓝图

> 本文是 GGYGO 以 Lyra 为参考的架构设计蓝图。分层记录每一层的**Lyra 事实**（读源码确认过的，不是印象）、**GGYGO 诉求**、**落地方案**和**待决策项**。
>
> 参考工程：`F:\ue_project\LyraStarterGame`（只读）。目标工程：`f:\ue_project\GGYGO`。
>
> 配套架构图：[[GGYGO_整体架构.canvas]]。图中节点颜色含义：绿=照抄 Lyra，青=GGYGO 自建或扩展，紫=现有代码保留，黄=UE/GAS 原生，橙=待改造或待拍板。

---

## 0. 已确认的架构决策

这四项已拍板，是后面所有设计的前提，不再作为开放选项讨论。

| # | 决策 | 内容 | 影响 |
|---|---|---|---|
| **D1** | **ASC 采用两级布局** | 每个角色 Pawn 各自持有一个 ASC + 独立 AttributeSet；PlayerState 额外持有一个队伍级 ASC | 所有 GAS 代码按"Owner=自己、Avatar=自己"写；队伍共享资源归 PlayerState 的 ASC |
| **D2** | **每个玩家各带一支队伍** | 联机时每名玩家拥有自己的完整队伍（多个角色 Pawn 常驻），队伍机制在联机下不关闭 | 4 人房 × 3 角色 = 12 个角色 ASC 同时复制，网络与性能预算必须按这个量级设计 |
| **D3** | **废弃现有 locomotion Pipeline，移动改用 CharacterMovementComponent** | `Pipeline/`、`Drivers/`、`StateMachine/`、`Components/GGYGOCharacterRuntimeComponent`、`Contracts/Pipeline`、`Contracts/State` 整体废弃；移动权威回归 CMC | 现有约 40 个文件退役，`ABaseCharacter::Tick` 唯一帧调度取消，ZZZAnim 的数据来源需要重建 |
| **D4** | **同优先级 GA 由后来者打断先激活者** | 仲裁比较用 `>` 而不是 `>=` | 同级技能可互断；普攻连段不能靠"同级先到先得"实现，必须靠组规则 `SingleInstanceQueued` 或段间不同 Priority |

D3 是这次最大的改动，它把项目从"自研纯 C++ Pipeline + 引擎对象薄壳"拉回到"UE/GAS 标准架构"。收益是获得移动复制与客户端预测、去掉一整套自维护时序、和 Lyra 参考完全同构；代价是现有已跑通的移动手感需要在新基础上重新调一遍。

---

## 1. 三条硬约束

所有设计都被这三条倒推：

| 约束 | 对架构的强制要求 |
|---|---|
| **动作游戏** | 帧级响应、输入缓冲、动作优先级与打断、命中判定窗口。GA 之间必须有可比较的优先级，不能只有二元的独占/非独占。 |
| **队伍多角色**（D1/D2） | 每个角色需要独立的属性、冷却、Buff；下场角色的 GE 必须继续运转；下场角色可由 AIController 接管参战。 |
| **多人联机**（D2） | 所有改变游戏状态的行为必须走 GAS 预测/复制链路，移动必须走 CMC。这是 D3 的直接原因。 |

三条里**多人联机是最强的**。它不是"以后再加"的特性——一旦有逻辑绕过复制链路，后期改造等于重写。

---

## 2. 分层总览

```
┌─ 输入层        Enhanced Input → InputTag                       [UE 输入框架 + Lyra 照抄]
├─ 意图层        InputTag/原始轴 → 语义意图 + 输入缓冲             [GGYGO 自建，Lyra 无]
├─ 仲裁层        意图 + 优先级 + 组规则 → 允许哪个动作             [GGYGO 扩展 Lyra 激活组]
├─ GAS 层        TryActivateAbility → GA/GE/Cue/Event            [Lyra 照抄 + GGYGO 扩展]
├─ 表现层        Montage / GameplayCue / AnimInstance 自拉        [GGYGO 接线]
└─ 移动层        UGGYGOCharacterMovementComponent (CMC 派生)      [D3 重建]
```

层间铁律：

1. **上层只产出请求，不直接改状态。** 意图层产出"想攻击"，不产出"已攻击"。
2. **状态变更的唯一入口是 GAS。** 除移动输入外，任何改变角色状态的行为都通过 `TryActivateAbility` 或 `ApplyGameplayEffect`。
3. **表现层只读。** AnimBP、Cue、UI 只消费状态，不反向写权威数据。
4. **移动层是唯一例外，权威是 CMC。** Locomotion 不走 GAS（和 Lyra 一致），但必须走 CMC 以获得复制与预测。动作位移走 RootMotion / RootMotionSource，同样由 CMC 应用。
5. **不存在第二个帧调度器。** CMC 自己 Tick、AnimInstance 自己 Update、GAS 自己驱动。D3 之后不再有"唯一角色 Tick 入口"这种自维护时序。

---

## 3. AbilitySystem 层：照抄 Lyra 的部分

### 3.1 可以直接照抄的清单

| Lyra 文件 | GGYGO 对应位置 | 照抄程度 | 说明 |
|---|---|---|---|
| `AbilitySystem/LyraAbilitySystemComponent.*` | `AbilitySystem/GGYGOAbilitySystemComponent.*` | 照抄 + 扩展 | 输入 Tag 缓存、`ProcessAbilityInput`、TagRelationship 保留；激活组部分重写，见第 5 章 |
| `AbilitySystem/LyraAbilitySet.*` | `AbilitySystem/GGYGOAbilitySet.*` | 直接照抄 | 成组授予 Ability/Effect/AttributeSet 并可整组回收，队伍换角色必需 |
| `AbilitySystem/LyraAbilitySystemGlobals.*` | `AbilitySystem/GGYGOAbilitySystemGlobals.*` | 直接照抄 | 只为了让自定义 EffectContext 生效 |
| `AbilitySystem/LyraGameplayEffectContext.*` | `AbilitySystem/GGYGOGameplayEffectContext.*` | 直接照抄 | 携带 AbilitySource 与命中结果，伤害结算必需 |
| `AbilitySystem/LyraAbilitySourceInterface.*` | 同名迁移 | 直接照抄 | 配合上面的 EffectContext |
| `AbilitySystem/LyraAbilityTagRelationshipMapping.*` | 同名迁移 | 直接照抄 | Tag 级 Block/Cancel 关系表，与第 5 章的组优先级互补 |
| `AbilitySystem/Abilities/LyraGameplayAbility.*` | `AbilitySystem/Abilities/GGYGOGameplayAbility.*` | 照抄 + 扩展 | 保留 ActivationPolicy、AdditionalCosts、失败反馈、相机模式；扩展优先级与组 |
| `AbilitySystem/Abilities/LyraAbilityCost*.*` | `AbilitySystem/Abilities/Costs/` | 照抄骨架 | Lyra 的三个实现绑定 Inventory，GGYGO 换成能量/耐力 |
| `AbilitySystem/Attributes/LyraAttributeSet.h` | `AbilitySystem/Attributes/GGYGOAttributeSet.h` | 直接照抄 | 基类 + `ATTRIBUTE_ACCESSORS` 宏 |
| `AbilitySystem/Attributes/LyraHealthSet.*` | `AbilitySystem/Attributes/GGYGOHealthSet.*` | 照抄 + 扩展 | Health/MaxHealth + Damage/Healing 元属性 + 死亡委托；再加韧性/削韧 |
| `AbilitySystem/Attributes/LyraCombatSet.*` | `AbilitySystem/Attributes/GGYGOCombatSet.*` | 直接照抄 | BaseDamage/BaseHeal |
| `AbilitySystem/Executions/LyraDamageExecution.cpp` | `AbilitySystem/Executions/GGYGODamageExecution.*` | 照抄结构 | 距离衰减换成动作游戏的部位倍率/防御结算 |
| `AbilitySystem/LyraGameplayCueManager.*` | `AbilitySystem/Cues/GGYGOGameplayCueManager.*` | 直接照抄 | 只做异步/预加载优化，不含 Cue 执行逻辑 |
| `AbilitySystem/LyraGlobalAbilitySystem.*` | `AbilitySystem/GGYGOGlobalAbilitySystem.*` | 照抄 | D1/D2 下价值很高：给整支队伍或全场角色广播 Buff |
| `AbilitySystem/Phases/LyraGamePhase*` | `AbilitySystem/Phases/` | 暂缓 | 用 Ability 表达关卡阶段，现阶段无需求 |
| `Character/LyraCharacterWithAbilities.*` | `Character/GGYGOCharacter.*` 的参考 | 重点参考 | **D1 下这是主要参考对象**，不是 `LyraCharacter`——每个角色自带 ASC |

### 3.2 现有代码处置（D3 生效后）

**废弃（整体退役）：**

| 现有路径 | 废弃原因 | 职责去向 |
|---|---|---|
| `Pipeline/CharacterControlPipeline.*` | 自维护帧时序取消 | CMC / AnimInstance / GAS 各自驱动 |
| `Pipeline/ArbiterPipeline.*` | 仲裁改由 GAS | GA 的 Tag 需求 + AbilityGroup 优先级 |
| `Pipeline/Arbiters/ActionArbiter.*` | 空实现，职责被组优先级取代 | 第 5 章仲裁算法 |
| `Pipeline/Arbiters/GASArbiter.*` | 读 Tag 写 bBlock 的中转层不再需要 | GA 的 `ActivationBlockedTags` 直接判断；移动封锁由 CMC 读 ASC Tag |
| `Pipeline/Arbiters/HealthArbiter.*` | `FDamageRequest` 队列是空转 | GE + `GGYGODamageExecution` |
| `Pipeline/Arbiters/StaminaArbiter.*` | 仅 TODO | AttributeSet 属性 + `GGYGOAbilityCost_Stamina` |
| `Pipeline/IntentPipeline.*` | 拆散重组 | 动作意图 → 意图层；移动意图 → `AddMovementInput` |
| `Pipeline/Intents/LocomotionIntentProcessor.*` | 移动方向直接给 CMC | `BindNativeAction` → `AddMovementInput` |
| `Pipeline/Intents/ViewRotationProcessor.*` | 视角直接给 Controller | `AddControllerYawInput` / `AddControllerPitchInput` |
| `Pipeline/Parameters/MovementParameterProcessor.*` | 速度/角度 CMC 自己有 | `CMC->Velocity` 等原生数据 |
| `Pipeline/Interfaces/*` | 三个纯虚接口失去实现者 | — |
| `Drivers/MotionDriver.*` | 直接写 Actor 位置在联机下不成立 | CMC 的 `Velocity` + RootMotion |
| `StateMachine/` 整套 | locomotion 状态由 CMC 推导，动作状态由 GAS Tag | `CMC->MovementMode` + 速度；`ASC` 的 `State.*` Tag |
| `Components/GGYGOCharacterRuntimeComponent.*` | Pipeline 宿主失去宿主对象 | `GGYGOPawnExtensionComponent` + `GGYGOHeroComponent` |
| `Contracts/Pipeline/CharacterFrameCommands.h` | 帧命令缓冲不再存在 | — |
| `Contracts/State/StateUpdateResult.h` | StateManager 退役 | — |
| `Data/Runtime/ArbiterRuntimeModel.h` | Block 标记改为直接查 ASC Tag | — |
| `Data/Runtime/StateRuntimeModel.h` | 同上 | — |
| `Data/Runtime/MovementRuntimeModel.h` | 速度/角度/接地 CMC 原生就有 | — |
| `Data/Runtime/IntentRuntimeModel.h` | 意图不再落盘为跨帧状态 | — |
| `Data/Runtime/ViewRuntimeModel.h` | ControlRotation 直接从 Controller 读 | — |
| `Data/Runtime/RuntimeData.h` | 聚合入口失去成员 | — |

**保留算法、迁移载体：**

| 现有路径 | 迁移目标 | 说明 |
|---|---|---|
| `Pipeline/Gait/GaitAuthorityProcessor.*` | `Movement/Components/GGYGOCharacterMovementComponent` | Walk→Run 持续时间升级的逻辑有价值，但步态是移动组件职责且需要复制 |
| `Pipeline/Parameters/RootMotionParameterProcessor.*` | 见第 10 章 | RM 曲线采样算法保留，输出改为 UE RootMotion 或 RootMotionSource |
| `Pipeline/Parameters/TurnBackPhaseProcessor.*` | `Movement/Components/GGYGOCharacterMovementComponent` | TurnBack 是移动状态，且需要复制 |
| `Pipeline/InputPipeline.*` 的输入缓冲部分 | `Pipeline/Intents/InputBufferQueue` | ActionBufferTime、防抖的思路保留给动作输入；Move/Look 不再需要双缓冲 |
| `Data/Runtime/GaitRuntimeModel.h` | CMC 内的复制字段 | `ResolvedGait` 需要复制给模拟端 |
| `Data/Runtime/RootMotionRuntimeModel.h` | 见第 10 章 | 曲线派生数据的归属随第 10 章的路径选择而定 |
| `Contracts/Animation/ZZZAnimRuntimeModel.h` | 保留，数据来源改变 | 不再由 Pipeline 推送，改由 AnimInstance 从 CMC + ASC 自行拉取 |

**改造后保留：**

| 现有路径 | 改造内容 |
|---|---|
| `Animation/zzzAnim/` 整套 | `UZZZAnimInstance::PipelineDrive` 推送模式改为 `NativeUpdateAnimation` 内自行从 CMC + ASC 拉取（Lyra `LyraAnimInstance` 的做法）。快照结构、Locomotion 决策/规则/事件的算法保留 |
| `Attributes/GGYGOAttributeSet.*` | 迁到 `AbilitySystem/Attributes/`，按 Lyra 拆成基类 + HealthSet + CombatSet |
| `GGYGOGameplayEffects.h/.cpp` | 迁到 `AbilitySystem/Effects/`，`InitGEGlobals()` 的同步加载改为接入 AssetManager 异步加载 |
| `GGYGOTags.h` | 迁到 `System/GGYGOGameplayTags.h`，`namespace + static const FName` + 运行时 `RequestGameplayTag` 换成 `UE_DEFINE_GAMEPLAY_TAG_STATIC` 原生声明（编译期注册、无字符串查找、可被 `.ini` 校验），`FGGYGOTagCache` 整个删除 |
| `Pipeline/Intents/AttackIntentProcessor.*`<br>`Pipeline/Intents/DodgeIntentProcessor.*` | 重写为意图层的 `ActionIntentResolver`，产出结构化意图而不是一个 bool |
| `Movement/MovementConfig.h` | 保留为 CMC 的配置结构；`RootMotionScale`、`TurnBack*` 等字段随第 10 章调整 |
| `BaseCharacter.*` / `PlayerCharacter.*` | 迁到 `Character/`，按 `LyraCharacterWithAbilities` 重写：自带 ASC，组件化拆分，去掉 Tick 调度和输入门面 |

**净影响**：约 25 个文件整体退役，约 8 个文件的算法迁移到新载体，约 7 个文件改造后保留。

---

## 4. 输入层与意图层

### 4.1 Lyra 的事实（已读源码确认）

Lyra 的输入分成**两条完全独立的路**：

```
路 A（动作 → GAS）：
  按键 → UInputAction → ULyraInputConfig 查表 → FGameplayTag(InputTag.*)
       → ULyraInputComponent::BindAbilityActions
       → ULyraHeroComponent::Input_AbilityInputTagPressed(InputTag)    [LyraHeroComponent.cpp:343]
       → ULyraAbilitySystemComponent::AbilityInputTagPressed(InputTag) [:351]
       → 按 Tag 扫描 Spec，缓存 Handle
       → ProcessAbilityInput（每帧）→ TryActivateAbility

路 B（移动/视角 → 不走 GAS）：
  按键 → BindNativeAction(InputTag_Move / Look_Mouse / Look_Stick / Crouch / AutoRun)
       → ULyraHeroComponent::Input_Move / Input_LookMouse ...           [:285-289]
       → 直接 AddMovementInput / AddControllerYawInput
```

**关键结论：Lyra 的移动完全不经过 GAS。** 只有跳跃、开火、交互这类"动作"走 Ability。D3 之后 GGYGO 与此完全一致。

两个值得抄的细节：
- `AddAdditionalInputConfig` / `RemoveAdditionalInputConfig`：运行时增删输入配置。**队伍换角色时正好用它换掉整套角色技能键位**。
- `NAME_BindInputsNow`：通过 `UGameFrameworkComponentManager` 广播的扩展事件，GameFeature 插件可以在这个时机注入输入绑定。

### 4.2 意图层：要有，但位置和职责必须收紧

**同意加意图层**，理由是 Lyra 的 `InputTag → Ability` 是一对一硬映射，动作游戏里同一个按键的语义依赖上下文：

- 轻攻击键在地面是普攻、空中是空袭、闪避后是反击
- 长按 0.3 秒变蓄力，连按变连段
- 方向键 + 闪避 = 定向闪避，无方向 = 后跳
- 攻击键在受击硬直窗口内按下，要进输入缓冲而不是丢弃

这些都是"一个物理输入 → 多种语义动作"，用 InputTag 直连表达不了，会退化成在每个 GA 的 `CanActivateAbility` 里塞上下文判断，逻辑分散且无法做输入缓冲。

**但意图层必须严格限制在"解释"，不能碰"执行"：**

| 意图层**可以**做 | 意图层**不可以**做 |
|---|---|
| 读原始输入与输入历史（长按时长、连按次数、方向） | 决定角色实际执行什么动作 |
| 读只读的上下文（CMC 的 MovementMode、ASC 的 Tag） | 修改 ASC 状态、应用 GE、改变属性 |
| 维护输入缓冲队列与有效窗口 | 直接改 Actor 位置或播放动画 |
| 产出结构化意图 `FGGYGOActionIntent` | 跳过 GAS 自己"执行"这个意图 |
| 把意图翻译成候选 Ability 的 InputTag | 决定最终哪个 Ability 胜出（那是仲裁层的事） |

**意图层的输出只有一件事：一个带优先级的候选动作请求，交给仲裁层，最终仍然通过 `TryActivateAbility` 进 GAS。**

这在联机下是硬要求。GAS 的客户端预测建立在"客户端调 `TryActivateAbility` → 生成 PredictionKey → 服务器验证"这条链上。如果意图层自己决定并执行了动作，就没有 PredictionKey，服务器无法验证，客户端也无法回滚。**意图层只跑在本地、只产出请求。**

### 4.3 落地结构

```
Input/
  GGYGOInputConfig.h/.cpp          照抄 Lyra，InputAction → InputTag 映射表
  GGYGOInputComponent.h/.cpp       照抄 Lyra，BindAbilityActions + BindNativeAction

Pipeline/Intents/                  （目录保留，内容重写）
  InputContextProbe.h/.cpp         只读采集上下文（CMC MovementMode、ASC Tag）
  ActionIntentResolver.h/.cpp      原始输入 + 上下文 → FGGYGOActionIntent
  InputBufferQueue.h/.cpp          输入缓冲：带时间窗的意图队列

Contracts/Intent/
  GGYGOActionIntent.h              意图结构体（纯数据）
```

意图结构草案：

```cpp
// Contracts/Intent/GGYGOActionIntent.h
USTRUCT()
struct FGGYGOActionIntent
{
    GENERATED_BODY()

    // 语义动作 Tag，如 Intent.Attack.Light / Intent.Attack.Charged / Intent.Dodge.Directional
    FGameplayTag IntentTag;

    // 意图产生的时刻，用于输入缓冲的有效窗判定
    float TimestampSeconds = 0.f;

    // 有效窗长度；超期未被消费则丢弃
    float BufferWindowSeconds = 0.f;

    // 方向意图（定向闪避、方向普攻），世界空间
    FVector WorldDirection = FVector::ZeroVector;

    // 蓄力/长按时长
    float HoldSeconds = 0.f;

    // 本次意图对应的候选 Ability 输入 Tag，交给仲裁层去 ASC 里找 Spec
    FGameplayTag AbilityInputTag;
};
```

**意图层不持有 Ability 引用，只产出 `AbilityInputTag`。** 这样它不依赖 GAS 类型，保持纯 C++、可测试；同时保留 Lyra 的 `AbilityInputTag → Spec` 查表机制，不重复造轮子。

### 4.4 移动输入

和 Lyra 一致：**移动/视角走 `BindNativeAction`，不进意图层，不进 GAS，直接 `AddMovementInput` / `AddControllerYawInput`。** 意图层只处理动作类输入（攻击、闪避、技能、大招、切人）。

---

## 5. GA 配置层：优先级 + 激活组 + 组规则

### 5.1 Lyra 的事实与局限

Lyra 有 `ELyraAbilityActivationGroup`，只有三个值：

```cpp
Independent            // 永不阻塞，也不被阻塞
Exclusive_Replaceable  // 可被其他 Exclusive 取消替换
Exclusive_Blocking     // 阻止所有其他 Exclusive 激活
```

判定实现（`LyraAbilitySystemComponent.cpp::IsActivationGroupBlocked`）是按枚举索引的固定数组 `ActivationGroupCounts[3]`，逻辑只有一句：**只要当前有任何 Blocking 能力，就拒绝所有新的 Exclusive**。

局限：

1. **只有一个"独占"概念，没有"组"。** 不能表达"技能之间互斥，但技能和普攻可以共存"。
2. **没有优先级数值。** `Exclusive_Replaceable` 之间是后到者无条件取消先到者——大招会被普攻打断。
3. **策略耦合在 GA 上。** 想改"技能组只能同时一个"这条规则，得改每个技能 GA 的配置。

### 5.2 方案：三个正交维度

| 维度 | 配在哪 | 类型 | 职责 |
|---|---|---|---|
| **组身份** `GroupTag` | GA 资产 | `FGameplayTag` | 这个 GA 属于哪个组。如 `AbilityGroup.Attack` / `.Skill` / `.Ultimate` / `.Dodge` / `.HitReact` |
| **组规则** `GroupRule` | 独立 DataAsset | 枚举 + 参数 | 该组内部的并发规则。**配在组上，不配在 GA 上**——改规则只改一处 |
| **自身策略** `SelfPolicy` + `Priority` | GA 资产 | 枚举 + int32 | 这个 GA 在组规则之上的个体行为，以及冲突时的强弱 |

**组规则**（`EGGYGOAbilityGroupRule`）：

| 值 | 语义 | 典型用途 |
|---|---|---|
| `Coexist` | 组内任意多个可同时激活 | Buff 类、被动 |
| `SingleInstance` | 组内同时只能一个，按优先级决定去留 | 技能组、大招组 |
| `SingleInstanceQueued` | 组内同时只能一个，新请求进缓冲队列而不是取消旧的 | **普攻连段**（等前一段结束再出下一段） |

**自身策略**（`EGGYGOAbilitySelfPolicy`）：

| 值 | 语义 |
|---|---|
| `Coexist` | 不主动排斥任何人，只受组规则约束 |
| `Exclusive` | 激活期间排斥**所有**组的低优先级 GA（不只是同组）。用于大招、死亡、被击倒 |

**优先级** `Priority`（int32，越大越强）。分段留空隙便于插值：

```
死亡 / 强制状态       1000
被击倒 / 强硬直        800
大招                  600
技能                  400
闪避                  300
重攻击                200
轻攻击                100
被动 / Buff             0
```

### 5.3 仲裁算法（含 D4）

把 Lyra 的固定数组换成按组 Tag 索引的实例表：

```cpp
// 取代 Lyra 的 ActivationGroupCounts[3]
TMap<FGameplayTag, TArray<TWeakObjectPtr<UGGYGOGameplayAbility>>> ActiveAbilitiesByGroup;
```

`CanActivateAbility` 阶段（只判断，不改状态）：

1. 取请求 GA 的 `GroupTag` / `Priority` / `SelfPolicy`
2. **全局排斥检查**：遍历所有已激活 GA，若存在 `SelfPolicy == Exclusive` 且 `Priority > 请求者Priority` 的实例 → 拒绝
3. **同组规则检查**：查 `GroupTag` 对应的 `GroupRule`
   - `Coexist` → 通过
   - `SingleInstance` → 若组内已有实例且其 `Priority > 请求者Priority` → 拒绝；否则通过（标记待取消列表）
   - `SingleInstanceQueued` → 若组内已有实例 → 拒绝激活，但把意图压回输入缓冲队列
4. **Tag 关系检查**：交给照抄来的 `GGYGOAbilityTagRelationshipMapping`，处理与组无关的 Tag 级 Block/Cancel

`NotifyAbilityActivated` 阶段（改状态）：

5. 把新实例登记进 `ActiveAbilitiesByGroup`
6. 执行第 3 步标记的取消列表（`CancelAbilityHandle`）
7. 若 `SelfPolicy == Exclusive`，取消所有 `Priority <` 自己的其他组实例

`OnAbilityEnded` 阶段：

8. 从 `ActiveAbilitiesByGroup` 摘除
9. 若该组规则是 `SingleInstanceQueued`，通知输入缓冲队列尝试出队

**D4 的落点在第 2、3 步：比较用 `>` 而不是 `>=`，即同优先级时后来者胜出并打断先激活者。**

这带来一个必须注意的后果：**普攻连段不能靠"同级先到先得"实现了**。第二段普攻如果和第一段同 Priority，会直接打断第一段，动画从头切。两个解法：

- 用 `SingleInstanceQueued` 组规则，让第二段进队列等第一段结束（推荐，语义清晰）
- 或给连段各段递增 Priority（100 / 101 / 102），靠优先级递增实现顺序推进

### 5.4 落地结构

```
AbilitySystem/
  GGYGOAbilitySystemComponent.h/.cpp     组表 + 仲裁实现
  Abilities/
    GGYGOGameplayAbility.h/.cpp          GroupTag / Priority / SelfPolicy 字段
  Groups/                                （新目录）
    GGYGOAbilityGroupConfig.h/.cpp       DataAsset：Map<GroupTag, FGGYGOAbilityGroupRule>
    GGYGOAbilityGroupTypes.h             枚举 + 规则结构体
```

---

## 6. GE 层

原本认为没有可抄的，实际上有三个值得抄：

1. **`LyraGameplayEffectContext`**：自定义 EffectContext，携带 `AbilitySource` 和命中的物理材质。"打到金属出火花、打到肉出血"就靠这个。
2. **`LyraDamageExecution`**：伤害结算用 `ExecutionCalculation` 而不是简单 Modifier。价值是能同时读源和目标的属性（攻击力 vs 防御力）、能读 EffectContext（命中部位）、能一次算完再写回。动作游戏的伤害公式必然需要。
3. **元属性模式**（`LyraHealthSet` 的 `Damage`/`Healing`）：GE 改的是 `Damage` 元属性，`PostGameplayEffectExecute` 里转换成 `Health` 扣减并清零。伤害可以被拦截、修正、触发事件，而不是直接扣血。**现有 `GGYGOAttributeSet::HandleIncomingDamage` 已经是这个模式**，保留并按 Lyra 补齐死亡委托。

GGYGO 需要自建的 GE 分类（放 `AbilitySystem/Effects/`）：

| 分类 | 用途 |
|---|---|
| 硬直 / 受击 | 施加 `Restriction.*` Tag + 持续时间，动作游戏的核心 |
| 削韧 / 破防 | 韧性属性的消耗与恢复 |
| 冷却 | 现有 `GGYGOGEs::Cooldown::*` 已有骨架 |
| 限制 | 现有 `GGYGOGEs::Restriction::*` 已有骨架 |
| 无敌帧 | 闪避的 i-frame，`Restriction.ImmuneDamage` |

---

## 7. GameplayEvent 层

直接照抄。GAS 原生的 `SendGameplayEventToActor` + `AbilityTask_WaitGameplayEvent` + `FGameplayEventData` 已经够用，Lyra 没有做额外封装。

动作游戏要额外接的两处：

1. **AnimNotify → GameplayEvent**：动画帧上的判定窗口、连段窗口、可取消点，用 AnimNotify 发 GameplayEvent，GA 内用 `WaitGameplayEvent` 接。这是动作游戏最常用的模式。**现有 `AnimNotify_CanYaw` 是同类需求的雏形**，但它走 `AnimInstance → RuntimeComponent → Pipeline` 直连转发；D3 之后这条转发链消失，新的动作类 Notify 一律走 GameplayEvent。
2. **命中 → GameplayEvent**：命中判定组件检测到命中后发事件，GA 决定要不要应用伤害 GE。

`GameplayMessageRuntime`（已在 `GGYGO.Build.cs` 声明）和 GameplayEvent 是两个不同东西：

| | GameplayEvent | GameplayMessage |
|---|---|---|
| 目标 | 特定 Actor 的 ASC | 全局广播，无目标 |
| 用途 | 驱动 Ability 逻辑 | 通知 UI / 音频 / 成就等旁观者 |
| 载荷 | `FGameplayEventData`（固定结构） | 任意 USTRUCT |

---

## 8. 表现层：GA 激活后怎么播动画和特效

### 8.1 事实澄清

**Lyra 确实没有专门的"GA 表现类"**，表现分散在三处：

1. GA 内部直接用 `AbilityTask_PlayMontageAndWait` 播 Montage
2. GE/GA 触发 `GameplayCue.*` Tag，由 `GameplayCueNotify` 处理特效音效
3. `LyraGameplayCueManager` 只负责 Cue 资源的异步/预加载优化，**不含任何 Cue 执行逻辑**，执行全在 GAS 原生

"GameplayCue"这个猜测是对的，它就是 GAS 的表现层机制。

### 8.2 三条表现通道

| 通道 | 机制 | 适用 | 联机行为 |
|---|---|---|---|
| **A. Montage** | GA 内 `PlayMontageAndWait` | 有独立完整动画的动作：攻击、闪避、技能 | Montage 由 GAS 复制（`NetExecutionPolicy` 决定），本地预测播放 |
| **B. GameplayCue** | Tag 触发 `GameplayCueNotify_Static`（一次性）/ `GameplayCueNotify_Actor`（持续） | 特效、音效、屏幕震动、命中反馈 | Cue 自带复制与本地预测 |
| **C. AnimInstance 自拉** | AnimInstance 在 `NativeUpdateAnimation` 里读 CMC + ASC，写自己的动画参数 | 需要和移动混合的表现：受击姿态、武器待机、上下半身分层 | 依赖 CMC / ASC 自身的复制 |

### 8.3 与现有 ZZZAnim 的关系（D3 生效后的改造）

现在是"Pipeline 推送"模式：`Pipeline::PublishAnimation` → `UZZZAnimInstance::PipelineDrive(snapshot)`。

D3 废弃 Pipeline 后，改为 Lyra 的"AnimInstance 自拉"模式：

```
UZZZAnimInstance::NativeUpdateAnimation
  ├─ 读 UGGYGOCharacterMovementComponent：Velocity、MovementMode、ResolvedGait、TurnBackPhase
  ├─ 读 UAbilitySystemComponent：State.* / Restriction.* Tag
  └─ 填充自己的快照结构 → ZZZ AnimBP 消费
```

**快照结构和 Locomotion 决策/规则/事件的算法全部保留**，只换数据来源。`Contracts/Animation/ZZZAnimRuntimeModel.h` 保留为快照定义，不再是"Pipeline 写、AnimInstance 读"的跨层投影契约。

单向只读的铁律不变：AnimInstance 不反向写 CMC 或 ASC 的权威数据。

### 8.4 需要搭的框架

```
AbilitySystem/
  Cues/
    GGYGOGameplayCueManager.h/.cpp          照抄 Lyra 的异步加载优化
    GGYGOGameplayCueNotify_Burst.h/.cpp     一次性 Cue 基类（命中、格挡、暴击）
    GGYGOGameplayCueNotify_Looping.h/.cpp   持续 Cue 基类（燃烧、蓄力光效）
  Tasks/
    AbilityTask_PlayMontageAndWaitForEvent.h/.cpp
        动作游戏必需：播 Montage 的同时等 AnimNotify 事件。
        GAS 官方示例（ActionRPG）有，Lyra 没有，需要移植。
    AbilityTask_WaitForHit.h/.cpp
        等命中判定组件的命中事件
```

先建空骨架，具体 Cue 资产以后按需填充。

---

## 9. 整体架构：Owner / Avatar 与队伍多角色（D1 / D2）

### 9.1 Lyra 的事实

Lyra 有两套布局：

**布局 1（玩家角色，默认）：ASC 挂 PlayerState**

```
ALyraPlayerState                     ← ASC 的 Owner
  ├─ ULyraAbilitySystemComponent     ← ASC 本体（LyraPlayerState.h:153）
  ├─ ULyraHealthSet / CombatSet
  └─ PawnData (Replicated)
        │ InitAbilityActorInfo(Owner=PlayerState, Avatar=Pawn)
        ▼
ALyraCharacter                       ← Avatar，GetAbilitySystemComponent() 转发到 PlayerState
  ├─ ULyraPawnExtensionComponent     ← InitState 状态机协调初始化顺序
  ├─ ULyraHeroComponent              ← 输入绑定、相机
  └─ ULyraHealthComponent            ← 把 HealthSet 包成组件 API
```

**布局 2（AI/NPC）：ASC 挂 Pawn 自身**

`ALyraCharacterWithAbilities` 自己持有 ASC + HealthSet + CombatSet，`PostInitializeComponents` 里自行 `InitAbilityActorInfo(this, this)`。

### 9.2 为什么布局 1 在 GGYGO 不成立

一个 ASC 只能挂**一套**同类型 AttributeSet。三个角色共用 PlayerState 上的一个 ASC，就是共享同一份 HP、同一份技能冷却——这不是绝区零，是换皮不换数据。

绝区零/原神的实际模型是：**每个角色有独立的 HP、能量、技能冷却、Buff，下场后继续走冷却和回能**。这要求每个角色一套独立的属性和 Ability 状态。

### 9.3 D1 采纳的布局：两级 ASC

```
AGGYGOPlayerState                    ← 队伍级 ASC 的 Owner
  ├─ UGGYGOAbilitySystemComponent    ← 队伍 ASC
  ├─ UGGYGOTeamAttributeSet          ← 队伍共享资源（切人能量、连携值）
  └─ 队伍 Ability（切人技、连携技、队伍 Buff）

每个角色 Pawn（同一支队伍的角色全部常驻世界）
  ├─ UGGYGOAbilitySystemComponent    ← 角色 ASC（Owner=自己，Avatar=自己）
  ├─ UGGYGOHealthSet / CombatSet     ← 角色独立属性
  └─ UGGYGOPawnExtensionComponent    ← InitState 协调（照抄 Lyra）

上场角色额外挂：
  └─ UGGYGOHeroComponent             ← 输入绑定、相机；切换时在角色间转移
```

主要参考对象是 `ALyraCharacterWithAbilities` 而不是 `ALyraCharacter`。

跨级查询规则要写清楚：角色 Ability 需要读队伍资源时，通过 `GetOwningPlayerState()` 拿队伍 ASC，不要把队伍属性复制进角色 AttributeSet。

### 9.4 D2 的网络预算

每个玩家各带一支队伍，意味着：

| 项 | 4 人房 × 每队 3 角色 |
|---|---|
| 角色 ASC | 12 |
| 队伍 ASC | 4 |
| 常驻角色 Pawn | 12 |

这个量级下必须做的事：

1. **下场角色降低复制频率。** 下场角色对其他玩家不可见时，用 `NetUpdateFrequency` 降频或 `NetDormancy` 休眠；但 GE 计时不能停（冷却、回能要继续），所以是"降低复制"而不是"停止模拟"。
2. **AttributeSet 复制模式。** GAS 的 `ReplicationMode` 三选：`Full`（单机/所有属性给所有人）、`Mixed`（属性只给所属客户端，GE 给所有人，玩家角色用这个）、`Minimal`（只给 GameplayTag 和 Cue，AI 用这个）。**队伍角色用 `Mixed`，敌人 AI 用 `Minimal`**。
3. **角色 Pawn 的相关性。** 下场角色如果不在场上表现，考虑不 spawn Pawn 而只保留一个轻量的"角色状态容器 Actor"（只带 ASC，不带 Mesh）。这是性能优化，先按全 Pawn 常驻做，压力测试后再决定。

`GGYGOGlobalAbilitySystem`（照抄 Lyra）在这里价值很高：给整支队伍广播 Buff 不需要遍历手写。

### 9.5 切换角色的流程

```
1. 输入触发切人 → 队伍 ASC 的 GA_SwitchCharacter（走 GAS，可被打断、有 CD、消耗切人能量）
2. GA 内：旧角色 UGGYGOHeroComponent 解绑输入（RemoveAdditionalInputConfig）
3. Controller UnPossess 旧角色 → Possess 新角色           ← 服务器权威，客户端不预测
4. 新角色 HeroComponent 绑定输入（AddAdditionalInputConfig，换成新角色技能键位）
5. 相机目标切换（CameraComponent 跟随新 Avatar）
6. 旧角色交给 AIController（要"同时上场战斗"）或进入待机（隐藏但 ASC 继续运转）
```

**第 3 步在联机下必须服务器权威。** `Possess` 只能在服务器执行，客户端只能预测表现层（相机、UI），不能预测控制权转移。切人的输入响应延迟因此等于一个 RTT，需要用表现层动画（切人特效、镜头推进）盖住。

---

## 10. 移动层重建（D3）

### 10.1 现有实现为什么必须换

`FMotionDriver` 直接调用 Actor 位移 API 提交移动，绕过 `UCharacterMovementComponent`。位移来源是 ZZZ 动画曲线（`RM_PosX` / `RM_PosY` / `RM_Yaw`）的采样差分。

联机下的三个致命问题：

1. **没有移动复制。** CMC 的 `ReplicateMoveToServer` / `ClientAdjustPosition` 全部被绕过，服务器和客户端各算各的，必然漂移。
2. **没有客户端预测与纠正。** 玩家在高延迟下会看到角色橡皮筋。
3. **服务器无法验证。** 作弊客户端可以任意改位置。

### 10.2 新结构

```
Movement/
  Components/
    GGYGOCharacterMovementComponent.h/.cpp
        UCharacterMovementComponent 派生。承载：
        - 步态权威（从 FGaitAuthorityProcessor 迁入，Walk→Run 持续时间升级）
        - TurnBack 阶段状态机（从 FTurnBackPhaseProcessor 迁入）
        - 上述状态的复制（模拟端也要知道步态和 TurnBack 阶段）
        - 读 ASC 的 Restriction.CantMove 决定是否接受输入
  MovementConfig.h                   保留为配置结构
```

预测数据的处理：步态和 TurnBack 阶段是"客户端会预测、服务器要验证"的状态，需要走 CMC 的 `FSavedMove_Character` / `FNetworkPredictionData_Client_Character` 扩展（自定义 SavedMove 保存这些字段并参与 `CanCombineWith` / `SetMoveFor` / `PrepMoveFor`）。这是 CMC 扩展的标准套路，也是这一层最容易出错的地方。

### 10.3 ZZZ 曲线位移的两条路径

ZZZ 的位移来自动画曲线，不是 UE 原生 RootMotion。要接进 CMC 有两条路：

**路径 a：离线烘焙成 UE 原生 RootMotion（推荐）**

把 `RM_PosX` / `RM_PosY` / `RM_Yaw` 曲线烘进 AnimSequence 的 RootMotion 轨道，勾 `EnableRootMotion`，CMC 通过 `RootMotionMode` 自动处理，自带复制与预测。

- 优点：完全走引擎标准路径；运行时零成本；`FRootMotionMovementParams` 的复制机制成熟
- 缺点：需要离线处理每个动画资产；运行时不能动态缩放/改向（除非用 `RootMotionScale`）
- 基础设施：`AAADocs/Scripts/` 下已有 `run_turnback_yaw_bake.py`、`fix_turnback_program_rotation.py`、`verify_turnback_program_rotation.py`，说明烘焙链路已经在做

**路径 b：运行时采样曲线并构造 RootMotionSource**

保留 `FRootMotionParameterProcessor` 的采样算法，输出改为 `FRootMotionSource_*` 交给 `CMC->ApplyRootMotionSource()`。

- 优点：运行时可动态调整（按输入修正方向、按状态缩放速度），现有 TurnBack 的"d0 用曲线方向、d1 用输入方向"这类逻辑可以保留
- 缺点：RootMotionSource 的复制需要正确实现 `NetSerialize` / `Matches` / `PrepareRootMotion`；比路径 a 复杂

**建议：路径 a 做主体，路径 b 处理需要运行时干预的特例。** TurnBack 的方向修正逻辑属于后者。这个选择在实施到该阶段时最终确认，因为它取决于烘焙脚本能覆盖多少情况。

### 10.4 locomotion 状态的新归属

| 原来 | 现在 |
|---|---|
| `FGYGOStateManager` 的 Idle / Moving | `CMC->Velocity.SizeSquared()` + `CMC->MovementMode` 推导 |
| `RuntimeData.Movement.bIsGrounded` | `CMC->IsMovingOnGround()` |
| `RuntimeData.Movement.CurrentSpeed` | `CMC->Velocity.Size2D()` |
| `RuntimeData.State.CurrentState`（动作状态） | ASC 的 `State.*` GameplayTag |
| `RuntimeData.Arbiter.bBlockMove` | CMC 直接查 ASC 的 `Restriction.CantMove` |

**不再需要单独的状态机对象。** locomotion 状态是 CMC 数据的派生视图，动作状态是 GAS Tag。

---

## 11. GameFeature 架构

### 11.1 Lyra 的模型

```
LyraExperienceDefinition (DataAsset)
  ├─ DefaultPawnData
  ├─ GameFeaturesToEnable[]
  └─ Actions[]
        ├─ AddComponents            ← 给指定 Actor 类加组件
        ├─ AddAbilities             ← 给 ASC 加 AbilitySet
        ├─ AddInputContextMapping   ← 加输入映射
        └─ AddWidgets               ← 加 UI

LyraExperienceManagerComponent（挂 GameState）
  └─ 加载 Experience → 激活 GameFeature → 执行 Actions → 广播就绪
```

**GameFeature 的价值是"按需装配"，代价是"初始化时序变复杂"。** Lyra 用 `UGameFrameworkComponentManager` 的 InitState 状态机（`ULyraPawnExtensionComponent`）协调"组件什么时候能用"，这是 Lyra 最难懂也最容易抄错的部分。

### 11.2 分两步走

**第一步（现在）：只抄 InitState 协调机制，不抄 GameFeature 插件化**

- 抄 `LyraPawnExtensionComponent` 的 InitState 状态机，协调 ASC / PawnData / HeroComponent 的初始化顺序
- 抄 `LyraHeroComponent` 的 `NAME_BindInputsNow` 扩展点
- 不建 GameFeature 插件，Ability 和输入直接由 PawnData + AbilitySet 静态配置

InitState 机制本身就有独立价值（解决"ASC 还没好，HeroComponent 就想绑输入"这类竞态），而且它是 GameFeature 的前置。现有 `BeginPlay` 里那条脆弱的手工顺序（`InitGEGlobals` → `InitAbilityActorInfo` → Pipeline 初始化 → `StateManager::Init` → `InitASC`）正好被 InitState 替换——D3 之后其中一半调用点本来就不存在了。

**第二步（角色数量上来之后）：把角色做成 GameFeature 插件**

- 每个角色一个 GameFeature 插件（角色 Ability、动画资产、输入配置、Cue）
- 队伍配置决定激活哪些插件
- D2 下这一步收益明显：4 人房 12 个角色，不做按需加载会把全部角色资产常驻内存

现在就上 GameFeature 收益是零（只有几个角色），成本是要同时调试 GameFeature 时序 + GAS 时序 + 新 CMC 时序三套东西。

### 11.3 落地结构

```
GameModes/
  GGYGOExperienceDefinition.h/.cpp        第二步再建
  GGYGOExperienceManagerComponent.h/.cpp  第二步再建
Character/
  GGYGOCharacter.h/.cpp                   第一步就建，参考 LyraCharacterWithAbilities
  Components/
    GGYGOPawnExtensionComponent.h/.cpp    第一步就建，InitState 协调
    GGYGOHeroComponent.h/.cpp             第一步就建，输入 + 相机
    GGYGOHealthComponent.h/.cpp           第一步就建，HealthSet 的组件 API
  Data/
    GGYGOPawnData.h/.cpp                  第一步就建，角色定义 DataAsset
Teams/
  GGYGOTeamComponent.h/.cpp               第九阶段，队伍成员管理与切换
```

---

## 12. 实施顺序

每一阶段结束都要能编译、能跑、能提交。

| 阶段 | 内容 | 产出验证 |
|---|---|---|
| **1. GAS 核心层** | 第 3.1 节的"直接照抄"项：ASC、AbilitySet、Globals、EffectContext、AbilitySourceInterface、TagRelationshipMapping、GameplayAbility 基类、AttributeSet 三件套 | 编译通过；角色能被授予一个空 GA 并激活 |
| **2. Tag 体系重写** | `GGYGOTags.h` → `System/GGYGOGameplayTags.h`，换 `UE_DEFINE_GAMEPLAY_TAG_STATIC`，删 `FGGYGOTagCache`，补 `AbilityGroup.*` / `Intent.*` / `GameplayCue.*` / `InputTag.*` 分类 | 编辑器 Tag 列表正确；无运行时字符串查找 |
| **3. 组优先级仲裁** | 第 5 章三维度模型 + `ActiveAbilitiesByGroup` + 仲裁算法（D4 用 `>`） + GroupConfig DataAsset | 两个不同优先级的 GA 互相打断行为符合预期；同级后来者能打断先来者 |
| **4. 角色组件化与 InitState** | `GGYGOCharacter`（自带 ASC）+ PawnExtension / Hero / Health / PawnData，替换现有 `BeginPlay` 手工顺序 | 初始化顺序不再依赖手工排列 |
| **5a. 移动层重建** | `UGGYGOCharacterMovementComponent`：步态权威 + TurnBack 迁入 + SavedMove 预测扩展；废弃 `Drivers/`、`StateMachine/`、`Pipeline/` 中的移动相关部分 | 角色能走能跑；步态切换正确；单机手感回到废弃前水平 |
| **5b. ZZZAnim 数据源改造** | `PipelineDrive` 推送改为 `NativeUpdateAnimation` 自拉；快照算法不变 | 动画表现与 5a 前一致 |
| **5c. Pipeline 整体退役** | 删除第 3.2 节"废弃"清单中的全部文件 | 编译通过；无残留引用 |
| **6. ZZZ 曲线位移接入 CMC** | 第 10.3 节路径 a 为主、路径 b 处理特例；TurnBack 方向修正逻辑重新实现 | TurnBack 手感恢复；位移不再直接写 Actor |
| **7. 输入与意图层** | InputConfig / InputComponent 照抄 + 意图层三个文件 + 输入缓冲 | 长按/连按/定向能产出正确意图；意图能激活对应 GA |
| **8. 表现层框架** | CueManager、两个 CueNotify 基类、`PlayMontageAndWaitForEvent` AbilityTask | 一个攻击 GA 能播 Montage 并触发一个 Cue |
| **9. 第一个完整动作** | GA_Attack_Light：Montage + 命中判定 + 伤害 GE + Cue + 硬直，位移走 RootMotion | 打得出来、打得到、有反馈、能被打断 |
| **10. 队伍多角色** | 第 9.3 节两级 ASC + 队伍 ASC + 切人 GA + HeroComponent 转移 + TeamComponent | 三个角色可切换，各自 HP/CD 独立，下场角色 CD 继续走 |
| **11. 联机** | Dedicated Server 起服；按第 9.4 节配复制模式；验证预测与纠正 | 两个客户端各带一队，看到一致的战斗表现 |
| **12. GameFeature 插件化** | 第 11.2 节第二步 | 角色资产按需加载 |

**第 5 阶段是最危险的一步**：5a / 5b / 5c 必须连着做完才能重新跑起来。中间状态下 ZZZAnim 会因为数据源被抽走而失效。建议这三步在同一个工作区连续完成，一次性提交。

---

## 13. 剩余待决策清单

D1～D4 已定。剩下这些不阻塞第 1 阶段开工，实施到对应阶段前确认即可。

| # | 问题 | 选项 | 需要在哪个阶段前定 |
|---|---|---|---|
| 1 | 普攻连段用哪种机制 | `SingleInstanceQueued` 组规则（推荐）/ 各段递增 Priority | 阶段 3 |
| 2 | ZZZ 曲线位移的实现路径 | 路径 a 离线烘焙（推荐）/ 路径 b 运行时 RootMotionSource / 两者混合 | 阶段 6 |
| 3 | 下场角色是否 spawn 完整 Pawn | 全 Pawn 常驻（先这样）/ 轻量状态容器 Actor | 阶段 10 |
| 4 | 队伍角色数量上限 | 3（绝区零）/ 4（原神） | 阶段 10 |
| 5 | 是否需要 `GGYGOGamePhase*` | 需要（关卡流程）/ 不需要 | 阶段 12 |
| 6 | 韧性/削韧是否做成独立 AttributeSet | 并入 HealthSet / 独立 `GGYGOPoiseSet` | 阶段 1 |

---

## 14. 与 Lyra 的差异总表

| 层 | Lyra 做法 | GGYGO 做法 | 性质 |
|---|---|---|---|
| ASC 归属 | PlayerState 单个 | 每角色 Pawn 一个 + PlayerState 队伍级一个（D1） | **重大改动** |
| 主要角色参考类 | `ALyraCharacter`（ASC 在 PlayerState） | `ALyraCharacterWithAbilities`（自带 ASC） | 参考对象不同 |
| 输入 → 动作 | InputTag 直连 Ability | InputTag → 意图层 → 仲裁 → Ability | **新增一层** |
| 移动输入 | `BindNativeAction`，不走 GAS | 同 Lyra，不走 GAS | 一致 |
| GA 并发控制 | 三值枚举 + 单组计数 | 组 Tag + 组规则 DataAsset + int32 优先级 + 自身策略 | **重大扩展** |
| 同级 GA 冲突 | 后到者无条件取消先到者 | 同上（D4 与 Lyra 行为一致），但有 Priority 分级和组规则约束 | 一致 + 扩展 |
| Tag 关系 | `AbilityTagRelationshipMapping` | 照抄，与组优先级并存互补 | 照抄 |
| 成本 | `LyraAbilityCost` 可插拔 | 照抄，换成能量/耐力 | 照抄 |
| 伤害 | `DamageExecution` + 元属性 | 照抄，公式换动作游戏的 | 照抄 |
| 表现 | Montage + GameplayCue + AnimInstance 自拉 | 同 Lyra；ZZZAnim 从推送模式改为自拉模式 | 照抄 + 改造现有 |
| 移动实现 | CMC 原生 + RootMotion | CMC 派生类承载步态与 TurnBack；ZZZ 曲线烘成 RootMotion（D3） | **废弃自研，回归标准** |
| 帧调度 | 各组件自行 Tick | 同 Lyra，取消唯一角色 Tick 入口（D3） | **废弃自研** |
| 组件初始化 | InitState 状态机 | 照抄 | 照抄 |
| 数据装配 | Experience + GameFeature | 分两步，先静态 PawnData，后 GameFeature | 照抄但延后 |
| 队伍切换 | 无此概念 | 队伍 ASC 的切人 GA + HeroComponent 转移 | **全新** |
| 联机形态 | 一玩家一角色 | 一玩家一支队伍（D2），4×3 量级 | **重大差异** |

---

## 附录 A：原始诉求原文

保留本文优化前的手写原文，作为需求出处。后续设计如与上文冲突，以上文经确认的决策（D1～D4）为准。

> 首先就是 abilitysystem，这个我认为基本可以照抄 lyra
>
> 然后输入方面，这方面我需要你和我一起考虑一下，就是 lyra 是直接的输入绑定 inputtag，然后根据 ability 的绑定的 tag 来找到 ability 激活的，但是我认为这样在动作游戏里面并不是很好，我认为还是需要一个类似于 input 转意图这样的意图管线，你可以参考一下我的意见，说出你的想法
>
> ga 配置方面，目前 lyra 除了 gas 自己的 ga 配置，他给 ga 配置了个激活组，我也会有类似的配置，但是我的需要改进的是因为我的这个是动作游戏所以不同技能 ga 之间是有优先级的，所以我打算给 ga 的配置上再加一层比如技能优先级 level，然后 ga 也有几乎组，然后就可以对比 ga 的优先级决定谁推出谁激活了，然后我认为可以做个类似于 ga 组的东西，然后封装规则，比如同组的 ga，比如这个是技能组，因为技能只能同时激活一个，所以这个组的规则就有同一组的 ga 激活的时候只保留一个，然后 ga 也可以在上面封装自己的激活规则，我目前想得就是独占，共存，这个你可以先计划着
>
> ge 因为我还没想到什么可以照抄 lyra 的，以及 gameevent 也可以直接照抄
>
> 关于表现，因为 lyra 里面没有 ga 表现类的或者说我还没看到所以我不知道怎么做，就是怎么这个 ga 激活之后 animinstance 去播放这个 ga 对应的动画，甚至还有例子特效那些，我听说是有个 gamecue，这个你先把框架搭好，以后我用到的时候直接填充
>
> 然后就是整体架构，他是逻辑和表现分层，也就是 owner 和 avatar 分开吧，我也可以这样做，我的游戏未来的需求就是我的角色可以像原神绝区零那样一个队伍有多个角色，我可以随意切换，甚至队伍里的角色同时上场战斗其他的 ai 战斗之类的，这个你需要考虑到
>
> 我的这个游戏计划的可以多人联机的游戏，这就是我是用 gas 的原因
>
> 我希望采用 gamefeature 架构

### 原文诉求到本文章节的对应

| 原文诉求 | 本文处理 | 结论摘要 |
|---|---|---|
| abilitysystem 照抄 lyra | 第 3 章 | 列出 16 项可照抄清单；主要参考对象是 `LyraCharacterWithAbilities` 而非 `LyraCharacter` |
| 输入转意图管线，要我的想法 | 第 4 章 | 同意加意图层，但严格限制在"解释"，输出 `FGGYGOActionIntent` + `AbilityInputTag`，最终仍走 `TryActivateAbility` 以保住 GAS 预测 |
| ga 加优先级 level + 激活组 + 组规则 + 独占/共存 | 第 5 章 | 拆成三个正交维度：组身份（GA 上）、组规则（独立 DataAsset）、自身策略 + `Priority`（GA 上）；D4 定为同级后来者打断 |
| ge 没想到可抄的 | 第 6 章 | 实际有三项值得抄：自定义 EffectContext、DamageExecution、元属性模式 |
| gameevent 直接照抄 | 第 7 章 | 同意；补充 AnimNotify → GameplayEvent 用法，并区分 GameplayEvent 与 GameplayMessage |
| ga 表现（动画/特效），听说有 gamecue，先搭框架 | 第 8 章 | 确认 Lyra 没有专门的 GA 表现类；给出三条通道与框架文件清单；ZZZAnim 改为自拉模式 |
| owner/avatar 分层 + 队伍多角色可切换 + 下场 AI 参战 | 第 9 章 | D1 采纳两级 ASC；D2 确认每玩家一支队伍，附网络预算与复制模式建议 |
| 多人联机（用 GAS 的原因） | 第 10 / 12 章 | D3 废弃自研 Pipeline，移动回归 CMC；联机验证列为阶段 11 |
| gamefeature 架构 | 第 11 章 | 同意但分两步：先只抄 InitState 协调机制，角色数量上来后再插件化 |
